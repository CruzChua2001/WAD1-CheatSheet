/**
 * Cheat Sheet Data - Web Application Development (Node, Express, HTML, JS)
 * Each item: category, item, syntax, description, returnType (optional), details, tag (HTML only)
 */
export const cheatSheetData = [
  // ========== JS Types & Quirks ==========
  {
    category: 'JS Types & Quirks',
    item: 'typeof null',
    syntax: 'typeof null',
    description: 'Type check for null value.',
    returnType: '"object"',
    details: 'typeof null returns "object" — this is a known JavaScript bug/historical quirk. Null is not an object.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'typeof undefined',
    syntax: 'typeof undefined',
    description: 'Type check for undefined.',
    returnType: '"undefined"',
    details: 'typeof undefined returns "undefined". Use when checking if a variable has not been assigned.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'typeof NaN',
    syntax: 'typeof NaN',
    description: 'Type check for NaN (Not a Number).',
    returnType: '"number"',
    details: 'typeof NaN returns "number". NaN is of type number but represents an invalid numeric result.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'typeof string',
    syntax: 'typeof "hello"',
    description: 'Type check for string.',
    returnType: '"string"',
    details: 'typeof returns "string" for string primitives and String values.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'typeof number',
    syntax: 'typeof 42',
    description: 'Type check for number.',
    returnType: '"number"',
    details: 'typeof returns "number" for numbers including NaN and Infinity.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'typeof boolean',
    syntax: 'typeof true',
    description: 'Type check for boolean.',
    returnType: '"boolean"',
    details: 'typeof returns "boolean" for true and false.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'typeof function',
    syntax: 'typeof function() {}',
    description: 'Type check for function.',
    returnType: '"function"',
    details: 'typeof returns "function" for functions. Functions are technically objects but typeof distinguishes them.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'typeof object',
    syntax: 'typeof {}  |  typeof []',
    description: 'Type check for object (and arrays).',
    returnType: '"object"',
    details: 'typeof {} and typeof [] both return "object". Use Array.isArray() to distinguish arrays.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'isNaN() vs Number.isNaN()',
    syntax: 'isNaN(x)  |  Number.isNaN(x)',
    description: 'Checking if a value is NaN.',
    returnType: 'boolean',
    details: 'isNaN() coerces the argument to a number first — isNaN("hello") is true. Number.isNaN() does not coerce — Number.isNaN("hello") is false. Use Number.isNaN() for strict NaN check.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'Object.is()',
    syntax: 'Object.is(a, b)',
    description: 'Strict equality check (same as === except NaN and ±0).',
    returnType: 'boolean',
    details: 'Object.is(NaN, NaN) is true; NaN === NaN is false. Object.is(0, -0) is false; 0 === -0 is true.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'Truthy values',
    syntax: 'Truthy: non-empty values that evaluate to true',
    description: 'Values that evaluate to true in boolean context.',
    returnType: 'N/A',
    details: 'Truthy: true, non-zero numbers, non-empty strings "", [], {}, (and most values). Falsy: false, 0, -0, 0n, "", null, undefined, NaN.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'Falsy values',
    syntax: 'Falsy: false, 0, "", null, undefined, NaN',
    description: 'Values that evaluate to false in boolean context.',
    returnType: 'N/A',
    details: 'The six falsy values: false, 0 (and -0, 0n), "" (empty string), null, undefined, NaN. Everything else is truthy.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'Loose equality (==)',
    syntax: 'a == b',
    description: 'Compares with type coercion.',
    returnType: 'boolean',
    details: '== (loose equality) coerces types before comparing. Example: 5 == "5" is true. Can lead to surprising results; prefer ===.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'Strict equality (===)',
    syntax: 'a === b',
    description: 'Compares without type coercion.',
    returnType: 'boolean',
    details: '=== (strict equality) compares value and type. No coercion. Example: 5 === "5" is false. Preferred for predictable comparisons.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'null vs undefined',
    syntax: 'null  |  undefined',
    description: 'Two "empty" values; different meanings.',
    returnType: 'N/A',
    details: 'undefined = not assigned / missing. null = intentionally empty. Both are falsy. typeof null is "object", typeof undefined is "undefined".',
  },
  {
    category: 'JS Types & Quirks',
    item: 'Primitive vs object',
    syntax: 'Primitives: string, number, boolean, null, undefined, symbol, bigint',
    description: 'Primitives are immutable and compared by value; objects by reference.',
    returnType: 'N/A',
    details: 'Primitives are not objects (except typeof null bug). Objects (including arrays, functions) are compared by reference.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'Boolean conversion',
    syntax: 'Boolean(x)  |  !!x',
    description: 'Convert any value to boolean.',
    returnType: 'boolean',
    details: 'Boolean(x) or !!x converts to true/false. Only falsy values become false; all others true.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'Array.isArray()',
    syntax: 'Array.isArray(x)',
    description: 'Check if value is an array.',
    returnType: 'boolean',
    details: 'typeof [] is "object". Array.isArray([]) is true, Array.isArray({}) is false. Use for reliable array check.',
  },
  {
    category: 'JS Types & Quirks',
    item: 'Optional chaining (?.)',
    syntax: 'obj?.prop  |  obj?.method()',
    description: 'Safely access nested property; short-circuits if null/undefined.',
    returnType: 'Property value or undefined',
    details: 'obj?.prop returns undefined if obj is null/undefined; otherwise obj.prop. Avoids "Cannot read property of undefined".',
  },
  {
    category: 'JS Types & Quirks',
    item: 'Nullish coalescing (??)',
    syntax: 'value ?? default',
    description: 'Use default only when value is null or undefined.',
    returnType: 'value or default',
    details: '?? returns right side only when left is null or undefined. Unlike ||, 0 and "" are kept. Use for fallbacks.',
  },

  // ========== JS Variables ==========
  {
    category: 'JS Variables',
    item: 'var',
    syntax: 'var x = 1;',
    description: 'Function-scoped, hoisted variable.',
    returnType: 'N/A',
    details: 'var is function-scoped (not block-scoped). It is hoisted: declaration is moved to top of function. Can be redeclared. Prefer let/const in modern JS.',
  },
  {
    category: 'JS Variables',
    item: 'let',
    syntax: 'let x = 1;',
    description: 'Block-scoped variable, can be reassigned.',
    returnType: 'N/A',
    details: 'let is block-scoped (inside {}). Not hoisted in same way as var (temporal dead zone). Can be reassigned but not redeclared in same scope.',
  },
  {
    category: 'JS Variables',
    item: 'const',
    syntax: 'const x = 1;',
    description: 'Block-scoped, immutable binding.',
    returnType: 'N/A',
    details: 'const is block-scoped. Must be initialized at declaration. The reference is immutable (cannot reassign), but object/array contents can still be mutated.',
  },
  {
    category: 'JS Variables',
    item: 'Object/Array assignment by reference',
    syntax: 'let a = [1,2]; let b = a;',
    description: 'Objects and arrays are assigned by reference, not by value.',
    returnType: 'N/A',
    details: 'When you assign an object or array to another variable, both point to the same reference. Mutating one affects the other. Use spread/copy to clone.',
  },
  {
    category: 'JS Variables',
    item: 'Temporal dead zone (TDZ)',
    syntax: 'let x = 1; { console.log(x); let x = 2; }',
    description: 'let/const are not accessible before declaration in block.',
    returnType: 'N/A',
    details: 'Accessing let or const before the line that declares it (in same block) throws ReferenceError. var would be undefined.',
  },
  {
    category: 'JS Variables',
    item: 'Block scope',
    syntax: '{ let x = 1; }  console.log(x); // ReferenceError',
    description: 'let and const are scoped to the block (any {}).',
    returnType: 'N/A',
    details: 'Block = if, for, while, switch body or bare {}. Variables inside are not visible outside. var is only function-scoped.',
  },
  {
    category: 'JS Variables',
    item: 'Redeclaration',
    syntax: 'var x = 1; var x = 2;  // OK.  let x = 1; let x = 2;  // SyntaxError',
    description: 'var can be redeclared; let/const cannot in same scope.',
    returnType: 'N/A',
    details: 'In same scope, let and const cannot be redeclared. var can, which can cause bugs.',
  },
  {
    category: 'JS Variables',
    item: 'Global scope',
    syntax: 'var in global becomes window.property (browser); let/const do not.',
    description: 'Top-level var attaches to global object; let/const do not.',
    returnType: 'N/A',
    details: 'In browser, var x at top level → window.x. let/const at top level are global but not on window.',
  },
  {
    category: 'JS Variables',
    item: 'Shallow copy array',
    syntax: 'const b = [...a];  or  const b = a.slice();',
    description: 'Copy array so mutations do not affect original.',
    returnType: 'N/A',
    details: 'Spread or slice() creates new array. Elements are still same references (shallow). For deep copy need recursive or library.',
  },
  {
    category: 'JS Variables',
    item: 'Shallow copy object',
    syntax: 'const b = { ...a };  or  Object.assign({}, a)',
    description: 'Copy object so mutations do not affect original.',
    returnType: 'N/A',
    details: 'Spread or Object.assign creates new object; nested objects are still shared (shallow copy).',
  },

  // ========== JS Loops ==========
  {
    category: 'JS Loops',
    item: 'for loop',
    syntax: 'for (let i = 0; i < arr.length; i++) { }',
    description: 'Classic loop with init, condition, update.',
    returnType: 'N/A',
    details: 'for (init; condition; update) — init runs once; condition checked each iteration; update after body. Use let/const for loop variable to keep block scope.',
  },
  {
    category: 'JS Loops',
    item: 'while loop',
    syntax: 'while (condition) { }',
    description: 'Repeats while condition is true.',
    returnType: 'N/A',
    details: 'Condition checked before each iteration. Ensure condition eventually becomes false to avoid infinite loop.',
  },
  {
    category: 'JS Loops',
    item: 'do...while loop',
    syntax: 'do { } while (condition);',
    description: 'Runs body at least once, then repeats while condition is true.',
    returnType: 'N/A',
    details: 'Condition checked after each iteration. Body always runs at least once.',
  },
  {
    category: 'JS Loops',
    item: 'for...of loop',
    syntax: 'for (const item of arr) { }',
    description: 'Iterates over iterable values (arrays, strings, etc.).',
    returnType: 'N/A',
    details: 'for...of gives each value (element). Use for arrays, strings, Map values, Set. Not for plain objects; use for...in or Object.keys().',
  },
  {
    category: 'JS Loops',
    item: 'for...in loop',
    syntax: 'for (const key in obj) { }',
    description: 'Iterates over enumerable property keys of an object.',
    returnType: 'N/A',
    details: 'for...in gives keys (property names). Use for plain objects. For arrays prefer for...of (for...in can include inherited/enumerable props).',
  },
  {
    category: 'JS Loops',
    item: 'break',
    syntax: 'break;',
    description: 'Exits the current loop or switch immediately.',
    returnType: 'N/A',
    details: 'break exits only the innermost loop/switch. Use labeled statement for nested: label: for(...) { break label; }.',
  },
  {
    category: 'JS Loops',
    item: 'continue',
    syntax: 'continue;',
    description: 'Skips to next iteration of the current loop.',
    returnType: 'N/A',
    details: 'continue skips rest of loop body for current iteration. In for loop, update expression still runs.',
  },
  {
    category: 'JS Loops',
    item: 'switch statement',
    syntax: 'switch (x) { case "a": ... break; default: ... }',
    description: 'Multi-way branch on value; use break to avoid fall-through.',
    returnType: 'N/A',
    details: 'switch compares with ===. Without break, execution falls through to next case. default runs if no match.',
  },
  {
    category: 'JS Loops',
    item: 'if / else',
    syntax: 'if (condition) { } else if (c2) { } else { }',
    description: 'Conditional execution.',
    returnType: 'N/A',
    details: 'condition is evaluated; truthy runs first block. else if and else optional. Use {} for multiple statements.',
  },

  // ========== JS Arrays & Strings ==========
  {
    category: 'JS Arrays & Strings',
    item: 'length',
    syntax: 'arr.length',
    description: 'Number of elements in the array (or string length).',
    returnType: 'number',
    details: 'Read-only for strings. For arrays, length is a property: setting arr.length = 0 truncates; setting larger extends (empty slots). arr.length returns number of elements.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'push',
    syntax: 'arr.push(item1, item2, ...)',
    description: 'Adds one or more elements to the end of an array.',
    returnType: 'New length of array (number)',
    details: 'Mutates the array. Returns the new length. push returns new length.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'pop',
    syntax: 'arr.pop()',
    description: 'Removes the last element from an array.',
    returnType: 'The removed element (or undefined if empty)',
    details: 'Mutates the array. pop returns the removed item.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'shift',
    syntax: 'arr.shift()',
    description: 'Removes the first element from an array.',
    returnType: 'The removed element (or undefined if empty)',
    details: 'Mutates the array. shift returns the removed item.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'unshift',
    syntax: 'arr.unshift(item1, item2, ...)',
    description: 'Adds one or more elements to the beginning of an array.',
    returnType: 'New length of array (number)',
    details: 'Mutates the array. unshift returns the new length.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'slice',
    syntax: 'arr.slice(start, end)',
    description: 'Returns a shallow copy of a portion of the array.',
    returnType: 'New array (copy)',
    details: 'Non-mutating. slice does not modify the original array. end is exclusive. Omit end to go to end.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'splice',
    syntax: 'arr.splice(start, deleteCount, ...items)',
    description: 'Changes array by removing/replacing elements and/or adding new elements.',
    returnType: 'Array of removed elements',
    details: 'Mutating. splice modifies the original array. Returns array of removed elements. Use for insert, delete, or replace in place.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'map',
    syntax: 'arr.map(callback)',
    description: 'Creates a new array with results of calling callback on every element.',
    returnType: 'New array',
    details: 'Non-mutating. map returns a new array of same length. Each element is the return value of the callback.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'filter',
    syntax: 'arr.filter(callback)',
    description: 'Creates a new array with elements that pass the callback test.',
    returnType: 'New array',
    details: 'Non-mutating. filter returns a new array containing only elements where callback returns truthy.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'reduce',
    syntax: 'arr.reduce(callback, initialValue)',
    description: 'Reduces array to a single value by applying callback left to right.',
    returnType: 'The accumulated value (any type)',
    details: 'reduce returns whatever the final accumulator value is. Callback receives (acc, current, index, array).',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'forEach',
    syntax: 'arr.forEach(callback)',
    description: 'Executes callback once for each array element.',
    returnType: 'undefined',
    details: 'forEach returns undefined. Use for side effects only; cannot break or return a value from forEach.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'join',
    syntax: 'arr.join(separator)',
    description: 'Joins all elements into a string with optional separator.',
    returnType: 'String',
    details: 'join returns a string. Default separator is comma. Empty string "" joins with no separator.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'split',
    syntax: 'str.split(separator, limit)',
    description: 'Splits a string into an array of substrings.',
    returnType: 'Array of strings',
    details: 'split returns an array. separator can be string or regex. limit caps the number of splits.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'indexOf',
    syntax: 'arr.indexOf(value)  |  str.indexOf(searchValue)',
    description: 'Returns first index at which value is found, or -1.',
    returnType: 'Number (index or -1)',
    details: 'indexOf returns the index of first occurrence, or -1 if not found. Uses strict equality (===).',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'includes',
    syntax: 'arr.includes(value)  |  str.includes(searchValue)',
    description: 'Returns true if array/string contains the value.',
    returnType: 'boolean',
    details: 'includes returns true or false. More readable than indexOf !== -1 for existence checks.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'concat',
    syntax: 'arr.concat(arr2, ...)  |  str.concat(str2, ...)',
    description: 'Combines arrays or strings; does not mutate original.',
    returnType: 'New array or string',
    details: 'concat returns a new array/string. Non-mutating. Prefer spread [...a, ...b] for arrays.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'reverse',
    syntax: 'arr.reverse()',
    description: 'Reverses array in place.',
    returnType: 'Same array (mutated)',
    details: 'Mutates the array. reverse returns a reference to the same array (reversed).',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'sort',
    syntax: 'arr.sort()  |  arr.sort(compareFn)',
    description: 'Sorts array in place. Without compareFn, sorts as strings.',
    returnType: 'Same array (mutated)',
    details: 'Mutates array. Default sort() converts to string and sorts lexicographically. For numbers use (a,b) => a - b.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'find',
    syntax: 'arr.find(callback)',
    description: 'Returns first element that satisfies callback, or undefined.',
    returnType: 'Element or undefined',
    details: 'find returns the first element where callback returns truthy. Stops when found. Returns undefined if none.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'findIndex',
    syntax: 'arr.findIndex(callback)',
    description: 'Returns index of first element that satisfies callback, or -1.',
    returnType: 'Number (index or -1)',
    details: 'findIndex returns index of first match, or -1. Like indexOf but with predicate.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'some',
    syntax: 'arr.some(callback)',
    description: 'Returns true if at least one element passes the test.',
    returnType: 'boolean',
    details: 'some returns true as soon as callback returns true for one element; otherwise false.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'every',
    syntax: 'arr.every(callback)',
    description: 'Returns true if all elements pass the test.',
    returnType: 'boolean',
    details: 'every returns false as soon as callback returns falsy for one element; otherwise true (empty array → true).',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'flat',
    syntax: 'arr.flat(depth)',
    description: 'Flattens nested arrays. Default depth 1.',
    returnType: 'New array',
    details: 'flat returns new array with subarrays concatenated. depth defaults to 1. flat(Infinity) for full flatten.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'flatMap',
    syntax: 'arr.flatMap(callback)',
    description: 'map then flat(1) in one step.',
    returnType: 'New array',
    details: 'flatMap runs callback and flattens result by one level. Equivalent to map().flat(1).',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'lastIndexOf',
    syntax: 'arr.lastIndexOf(value)  |  str.lastIndexOf(searchValue)',
    description: 'Returns last index at which value is found, or -1.',
    returnType: 'Number (index or -1)',
    details: 'lastIndexOf searches from end. Returns last occurrence index or -1.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'trim',
    syntax: 'str.trim()  |  str.trimStart()  |  str.trimEnd()',
    description: 'Removes whitespace from ends of string.',
    returnType: 'New string',
    details: 'trim returns new string with leading/trailing whitespace removed. Non-mutating. trimStart/trimEnd for one side.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'toLowerCase / toUpperCase',
    syntax: 'str.toLowerCase()  |  str.toUpperCase()',
    description: 'Returns string in lower or upper case.',
    returnType: 'New string',
    details: 'Both return new string; do not mutate original. Useful for case-insensitive comparison.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'startsWith / endsWith',
    syntax: 'str.startsWith(s)  |  str.endsWith(s)',
    description: 'Check if string starts or ends with substring.',
    returnType: 'boolean',
    details: 'Optional second argument is position to start checking from.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'replace',
    syntax: 'str.replace(search, replacement)',
    description: 'Replaces first match of search (string or regex) with replacement.',
    returnType: 'New string',
    details: 'replace returns new string. Only first match replaced unless regex with g flag. Non-mutating.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'substring / substr / slice (string)',
    syntax: 'str.substring(start, end)  |  str.slice(start, end)',
    description: 'Extract portion of string. end exclusive.',
    returnType: 'New string',
    details: 'substring and slice similar; slice can use negative indices. substr is deprecated. All return new string.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'fill',
    syntax: 'arr.fill(value, start, end)',
    description: 'Fills array with value (optionally from start to end).',
    returnType: 'Same array (mutated)',
    details: 'Mutates array. start/end optional; end exclusive. Returns the array. Useful for initializing.' },
  {
    category: 'JS Arrays & Strings',
    item: 'copyWithin',
    syntax: 'arr.copyWithin(target, start, end)',
    description: 'Copies portion of array to another position within same array.',
    returnType: 'Same array (mutated)',
    details: 'Mutates array. target = where to copy to; start/end = source range (end exclusive). Returns the array.',
  },
  {
    category: 'JS Arrays & Strings',
    item: 'toString (array)', syntax: 'arr.toString()',
    description: 'Returns string of array elements joined by comma.',
    returnType: 'string',
    details: 'Same as arr.join(). Calls toString on each element. Empty array returns "".',
  },

  // ========== JS Regex & Character ==========
  {
    category: 'JS Regex & Character',
    item: 'isNum(char)',
    syntax: 'function isNum(char) {\n  return /^[0-9]$/.test(char);\n}\n// or: char.length === 1 && char >= "0" && char <= "9"',
    description: 'Check if character is a digit (0–9).',
    returnType: 'boolean',
    details: 'Use /^[0-9]$/ so exactly one character is tested. For full string of digits use /^[0-9]+$/.',
  },
  {
    category: 'JS Regex & Character',
    item: 'isAlpha(char)',
    syntax: 'function isAlpha(char) {\n  return /^[a-zA-Z]$/.test(char);\n}\n// or: /^[A-Za-z]$/.test(char)',
    description: 'Check if character is a letter (a–z or A–Z).',
    returnType: 'boolean',
    details: 'Single character only. For Unicode letters use /^\\p{L}$/u (if supported).',
  },
  {
    category: 'JS Regex & Character',
    item: 'isUpper(char)',
    syntax: 'function isUpper(char) {\n  return /^[A-Z]$/.test(char);\n}\n// or: char === char.toUpperCase() && char !== char.toLowerCase()',
    description: 'Check if character is uppercase (A–Z).',
    returnType: 'boolean',
    details: 'Regex /^[A-Z]$/ for one uppercase letter. toUpperCase/toLowerCase trick works for any script.',
  },
  {
    category: 'JS Regex & Character',
    item: 'isLower(char)',
    syntax: 'function isLower(char) {\n  return /^[a-z]$/.test(char);\n}\n// or: char === char.toLowerCase() && char !== char.toUpperCase()',
    description: 'Check if character is lowercase (a–z).',
    returnType: 'boolean',
    details: 'Regex /^[a-z]$/ for one lowercase letter. toLowerCase/toUpperCase works for Unicode.',
  },
  {
    category: 'JS Regex & Character',
    item: 'isAlNum(char)',
    syntax: 'function isAlNum(char) {\n  return /^[a-zA-Z0-9]$/.test(char);\n}\n// or: /^[\\w]$/.test(char) — \\w is [a-zA-Z0-9_]',
    description: 'Check if character is alphanumeric (letter or digit).',
    returnType: 'boolean',
    details: '\\w in regex also includes underscore. Use [a-zA-Z0-9] for strict alphanumeric only.',
  },
  {
    category: 'JS Regex & Character',
    item: 'isSymbol(char)',
    syntax: 'function isSymbol(char) {\n  return /^[!@#$]$/.test(char);\n}',
    description: 'Check if character is one of the allowed symbols (!, @, #, $).',
    returnType: 'boolean',
    details: 'Returns true only for !, @, #, $. Add more symbols in the character class if needed (e.g. [!@#$%^&*]). Useful for password strength.',
  },

  // ========== Math & Number ==========
  {
    category: 'Math & Number',
    item: 'Round up (ceil)',
    syntax: 'Math.ceil(x)',
    description: 'Rounds up to the nearest integer.',
    returnType: 'number',
    details: 'Math.ceil(4.2) is 5, Math.ceil(-4.2) is -4. Always rounds toward positive infinity.',
  },
  {
    category: 'Math & Number',
    item: 'Round down (floor)',
    syntax: 'Math.floor(x)',
    description: 'Rounds down to the nearest integer.',
    returnType: 'number',
    details: 'Math.floor(4.8) is 4, Math.floor(-4.8) is -5. Always rounds toward negative infinity.',
  },
  {
    category: 'Math & Number',
    item: 'toFixed',
    syntax: 'num.toFixed(digits)',
    description: 'Formats number to a string with fixed decimal places.',
    returnType: 'string',
    details: 'num.toFixed(2) returns e.g. "3.14". digits 0–20. Rounds if needed. Returns string, not number.',
  },
  {
    category: 'Math & Number',
    item: 'Math.abs',
    syntax: 'Math.abs(x)',
    description: 'Absolute value (removes sign).',
    returnType: 'number',
    details: 'Math.abs(-5) is 5, Math.abs(5) is 5. Converts to number; Math.abs(" -3 ") is 3.',
  },
  {
    category: 'Math & Number',
    item: 'Math.random',
    syntax: 'Math.random()',
    description: 'Random decimal in range [0, 1) (0 inclusive, 1 exclusive).',
    returnType: 'number',
    details: 'Returns a number >= 0 and < 1. Multiply and add for range: min + Math.random() * (max - min) for [min, max).',
  },
  {
    category: 'Math & Number',
    item: 'Random in range (decimal)',
    syntax: 'min + Math.random() * (max - min)',
    description: 'Random number in [min, max) (min inclusive, max exclusive).',
    returnType: 'number',
    details: 'For [0, 10): Math.random() * 10. For [5, 15): 5 + Math.random() * 10. Max is exclusive.',
  },
  {
    category: 'Math & Number',
    item: 'Random integer (inclusive)',
    syntax: 'Math.floor(Math.random() * (max - min + 1)) + min',
    description: 'Random integer in [min, max] (both inclusive).',
    returnType: 'number',
    details: 'For 1–6 (dice): Math.floor(Math.random() * 6) + 1. For min–max inclusive: Math.floor(Math.random() * (max - min + 1)) + min.',
  },
  {
    category: 'Math & Number',
    item: 'Math.round',
    syntax: 'Math.round(x)',
    description: 'Rounds to nearest integer (0.5 rounds up).',
    returnType: 'number',
    details: 'Math.round(4.5) is 5, Math.round(4.4) is 4. Uses "round half up" (away from zero for negatives).',
  },
  {
    category: 'Math & Number',
    item: 'Math.trunc',
    syntax: 'Math.trunc(x)',
    description: 'Drops decimal part (truncate toward zero).',
    returnType: 'number',
    details: 'Math.trunc(4.9) is 4, Math.trunc(-4.9) is -4. No rounding; just removes fractional part.',
  },
  {
    category: 'Math & Number',
    item: 'Math.min / Math.max',
    syntax: 'Math.min(a, b, ...)  |  Math.max(a, b, ...)',
    description: 'Smallest or largest of arguments.',
    returnType: 'number',
    details: 'Math.min(1, 5, 3) is 1; Math.max(1, 5, 3) is 5. With array: Math.min(...arr). Returns Infinity / -Infinity if no args.',
  },
  {
    category: 'Math & Number',
    item: 'Math.sign',
    syntax: 'Math.sign(x)',
    description: 'Sign of number: 1, -1, 0, -0, or NaN.',
    returnType: 'number',
    details: 'Math.sign(5) is 1, Math.sign(-5) is -1, Math.sign(0) is 0. Useful for conditional logic on sign.',
  },
  {
    category: 'Math & Number',
    item: 'Math.sqrt',
    syntax: 'Math.sqrt(x)',
    description: 'Square root of x.',
    returnType: 'number',
    details: 'Math.sqrt(9) is 3. Returns NaN for negative. For cube root use Math.cbrt(x) or x ** (1/3).',
  },
  {
    category: 'Math & Number',
    item: 'Math.pow / exponent (**)',
    syntax: 'Math.pow(base, exp)  |  base ** exp',
    description: 'Base raised to exponent.',
    returnType: 'number',
    details: 'Math.pow(2, 3) is 8. Prefer 2 ** 3 in modern JS. ** is right-associative (2 ** 3 ** 2 = 2 ** 9).',
  },
  {
    category: 'Math & Number',
    item: 'Math constants (PI, E)',
    syntax: 'Math.PI  |  Math.E',
    description: 'Pi and Euler\'s number.',
    returnType: 'number',
    details: 'Math.PI ≈ 3.14159…, Math.E ≈ 2.71828…. Read-only. Use for geometry and natural log.',
  },

  // ========== Algorithm (common easy–medium) ==========
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Two Sum',
    syntax: 'function twoSum(arr, target) {\n  const map = {};\n  for (let i = 0; i < arr.length; i++) {\n    const need = target - arr[i];\n    if (map[need] !== undefined) return [map[need], i];\n    map[arr[i]] = i;\n  }\n  return undefined;\n}',
    description: 'Find two indices where values add up to target. One pass with hash map.',
    returnType: 'array of two indices or undefined',
    details: 'Store each value → index in object. For current arr[i], check if (target - arr[i]) exists in map; if yes return [map[need], i]. O(n) time, O(n) space.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Simulation',
    item: 'FizzBuzz',
    syntax: 'function fizzBuzz(n) {\n  for (let i = 1; i <= n; i++) {\n    let out = "";\n    if (i % 3 === 0) out += "Fizz";\n    if (i % 5 === 0) out += "Buzz";\n    console.log(out || i);\n  }\n}',
    description: 'Print Fizz for multiples of 3, Buzz for 5, FizzBuzz for both; else the number.',
    returnType: 'N/A (side effect)',
    details: 'Loop 1 to n. Build string from divisibility; if empty use i. Can use (i % 15 === 0) for FizzBuzz to avoid two ifs.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Reverse a string',
    syntax: 'function reverseString(str) {\n  return str.split("").reverse().join("");\n}\n// or: return [...str].reverse().join("");',
    description: 'Return string with characters in reverse order.',
    returnType: 'string',
    details: 'split("") gives array of chars; reverse() in place; join("") back to string. Alternative: loop from str.length-1 to 0 and concatenate. O(n) time.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Palindrome check',
    syntax: 'function isPalindrome(str) {\n  const s = str.toLowerCase().replace(/[^a-z0-9]/g, "");\n  return s === s.split("").reverse().join("");\n}',
    description: 'Check if string reads same forwards and backwards (ignore case and non-alphanumeric).',
    returnType: 'boolean',
    details: 'Normalize: toLowerCase, remove non-alphanumeric with regex. Compare with reversed. Or use two pointers (left++, right--) while left < right.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'DP',
    item: 'Fibonacci (nth number)',
    syntax: 'function fib(n) {\n  if (n <= 1) return n;\n  let a = 0, b = 1;\n  for (let i = 2; i <= n; i++) [a, b] = [b, a + b];\n  return b;\n}',
    description: 'Return nth Fibonacci number (0, 1, 1, 2, 3, 5, …). Iterative O(n), no stack overflow.',
    returnType: 'number',
    details: 'Iterative: keep prev and curr; update in loop. Recursive fib(n) = fib(n-1)+fib(n-2) is O(2^n) unless memoized. Memoization gives O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Factorial',
    syntax: 'function factorial(n) {\n  let result = 1;\n  for (let i = 2; i <= n; i++) result *= i;\n  return result;\n}\n// recursive: return n <= 1 ? 1 : n * factorial(n - 1);',
    description: 'Return n! (n × (n-1) × … × 1).',
    returnType: 'number',
    details: 'Iterative: loop from 2 to n, multiply. Recursive: base n <= 1 return 1; else return n * factorial(n-1). Watch stack for large n.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Find max / min in array',
    syntax: 'function findMax(arr) { return Math.max(...arr); }\nfunction findMin(arr) { return Math.min(...arr); }\n// or: arr.reduce((a, b) => Math.max(a, b))',
    description: 'Get largest or smallest value in array.',
    returnType: 'number',
    details: 'Spread: Math.max(...arr). Empty array → -Infinity/Infinity. Reduce: arr.reduce((acc, x) => x > acc ? x : acc, arr[0]). Loop works too.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Remove duplicates from array',
    syntax: 'function removeDuplicates(arr) {\n  return [...new Set(arr)];\n}\n// or: arr.filter((x, i) => arr.indexOf(x) === i)',
    description: 'Return array with unique values only.',
    returnType: 'array',
    details: 'Set stores unique values; spread back to array. filter + indexOf keeps first occurrence (O(n²)). Set approach is O(n). Preserves order with Set.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Merge two sorted arrays',
    syntax: 'function mergeSorted(a, b) {\n  let i = 0, j = 0, out = [];\n  while (i < a.length && j < b.length)\n    out.push(a[i] <= b[j] ? a[i++] : b[j++]);\n  return out.concat(a.slice(i), b.slice(j));\n}',
    description: 'Merge two sorted arrays into one sorted array (two pointers).',
    returnType: 'array',
    details: 'Two pointers i, j. Push smaller of a[i], b[j] and advance. When one exhausts, concat the rest. O(n + m) time.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Binary Search',
    item: 'Binary search (sorted array)',
    syntax: 'function binarySearch(arr, target) {\n  let left = 0, right = arr.length - 1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    if (arr[mid] === target) return mid;\n    if (arr[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return -1;\n}',
    description: 'Find index of target in sorted array in O(log n).',
    returnType: 'number (index or -1)',
    details: 'Maintain [left, right]. Mid = floor((left+right)/2). If arr[mid] === target return mid. Else narrow range. left > right → not found, return -1.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Linear search',
    syntax: 'function linearSearch(arr, target) {\n  return arr.indexOf(target);\n}\n// or: arr.findIndex(x => x === target);\n// or: for (let i = 0; i < arr.length; i++) if (arr[i] === target) return i; return -1;',
    description: 'Find first index of target in array.',
    returnType: 'number (index or -1)',
    details: 'indexOf(target) or findIndex. Manual loop: for (let i = 0; i < arr.length; i++) if (arr[i] === target) return i; return -1. O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Count vowels in string',
    syntax: 'function countVowels(str) {\n  return str.match(/[aeiou]/gi)?.length ?? 0;\n}\n// or: [...str].filter(c => /[aeiou]/i.test(c)).length',
    description: 'Count how many vowels (a,e,i,o,u) in a string (case-insensitive).',
    returnType: 'number',
    details: 'Regex /[aeiou]/gi: g = all matches, i = case insensitive. match returns array or null; use ?.length ?? 0. Or filter with test.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Anagram check',
    syntax: 'function isAnagram(a, b) {\n  const norm = (s) => s.split("").sort().join("");\n  return norm(a) === norm(b);\n}',
    description: 'Check if two strings have same characters (same frequency).',
    returnType: 'boolean',
    details: 'Sort both and compare (O(n log n)). Or: count chars in object for first string, decrement for second; all counts should be 0. O(n) with count.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Capitalize first letter of each word',
    syntax: 'function capitalizeWords(str) {\n  return str.split(" ").map(w => w[0].toUpperCase() + w.slice(1).toLowerCase()).join(" ");\n}',
    description: 'Return string with first letter of each word uppercase, rest lowercase.',
    returnType: 'string',
    details: 'Split by space, map each word to first char toUpperCase + rest slice(1).toLowerCase(), join. Handle multiple spaces if needed (filter empty).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Longest word in string',
    syntax: 'function longestWord(str) {\n  return str.split(" ").reduce((longest, w) => w.length > longest.length ? w : longest, "");\n}',
    description: 'Return the longest word in a sentence.',
    returnType: 'string',
    details: 'Split by space. Reduce: keep word with greater length. Or sort by length and take first: str.split(" ").sort((a,b) => b.length - a.length)[0].',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Sum of array',
    syntax: 'function sum(arr) {\n  return arr.reduce((acc, x) => acc + x, 0);\n}',
    description: 'Return sum of all numbers in array.',
    returnType: 'number',
    details: 'reduce with initial 0. Empty array → 0. For integers/floats this is standard. Watch for very large numbers (precision).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Flatten array (nested)',
    syntax: 'function flatten(arr) {\n  return arr.flat(Infinity);\n}\n// or recursive:\nfunction flattenRec(arr) {\n  return arr.flatMap(x => Array.isArray(x) ? flattenRec(x) : [x]);\n}',
    description: 'Convert nested arrays to single-level array.',
    returnType: 'array',
    details: 'flat(Infinity) flattens any depth. Recursive: if element is array, flatten it and concat; else wrap in array. flatMap + recursion for one level per step.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Chunk array into size n',
    syntax: 'function chunk(arr, size) {\n  const out = [];\n  for (let i = 0; i < arr.length; i += size)\n    out.push(arr.slice(i, i + size));\n  return out;\n}',
    description: 'Split array into subarrays of given size.',
    returnType: 'array of arrays',
    details: 'Loop i by size; push arr.slice(i, i + size). Last chunk may be smaller. Alternative: while (arr.length) out.push(arr.splice(0, size)) (mutates).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Sorting',
    item: 'Bubble sort',
    syntax: 'function bubbleSort(arr) {\n  for (let i = 0; i < arr.length; i++)\n    for (let j = 0; j < arr.length - 1 - i; j++)\n      if (arr[j] > arr[j + 1]) [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];\n  return arr;\n}',
    description: 'Sort array by repeatedly swapping adjacent if out of order.',
    returnType: 'same array (mutated)',
    details: 'Inner loop pushes largest to end each pass; shorten inner bound by i. O(n²). Prefer arr.sort((a,b) => a - b) in practice.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Stack',
    item: 'Valid parentheses',
    syntax: 'function isValidParentheses(str) {\n  const stack = [];\n  const pair = { ")": "(", "}": "{", "]": "[" };\n  for (const c of str) {\n    if (c in pair) { if (stack.pop() !== pair[c]) return false; }\n    else stack.push(c);\n  }\n  return stack.length === 0;\n}',
    description: 'Check if string has matching opening/closing brackets ( ), { }, [ ].',
    returnType: 'boolean',
    details: 'Stack: push opening brackets; for closing, pop and check it matches. Invalid if pop !== matching open or stack non-empty at end. O(n) time.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Find missing number (0 to n)',
    syntax: 'function findMissingNumber(arr) {\n  const n = arr.length;\n  const expected = n * (n + 1) / 2;\n  const actual = arr.reduce((a, b) => a + b, 0);\n  return expected - actual;\n}',
    description: 'Array has n numbers from 0..n with one missing; return the missing number.',
    returnType: 'number',
    details: 'Sum 0+1+…+n = n(n+1)/2. Subtract sum of array to get missing. Alternative: XOR all indices and values; result is missing number (no overflow).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Two Pointers',
    item: 'Three Sum (unique triplets)',
    syntax: 'function threeSum(arr, target = 0) {\n  arr.sort((a, b) => a - b);\n  const out = [];\n  for (let i = 0; i < arr.length - 2; i++) {\n    if (i > 0 && arr[i] === arr[i - 1]) continue;\n    let left = i + 1, right = arr.length - 1;\n    while (left < right) {\n      const sum = arr[i] + arr[left] + arr[right];\n      if (sum === target) {\n        out.push([arr[i], arr[left], arr[right]]);\n        while (left < right && arr[left] === arr[left + 1]) left++;\n        while (left < right && arr[right] === arr[right - 1]) right--;\n        left++; right--;\n      } else if (sum < target) left++;\n      else right--;\n    }\n  }\n  return out;\n}',
    description: 'Find all unique triplets that sum to 0 (or target). Sort + two pointers.',
    returnType: 'array of triplets',
    details: 'Sort array. For each arr[i], use two pointers on rest: left = i+1, right = end. Sum = arr[i]+arr[left]+arr[right]. If < 0 left++; if > 0 right--. Skip duplicate values for i, left, right. O(n²).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Move zeros to end',
    syntax: 'function moveZeros(arr) {\n  let w = 0;\n  for (let i = 0; i < arr.length; i++)\n    if (arr[i] !== 0) arr[w++] = arr[i];\n  while (w < arr.length) arr[w++] = 0;\n  return arr;\n}',
    description: 'Move all 0s to the end of the array in place.',
    returnType: 'same array (mutated)',
    details: 'Write pointer w: copy non-zero to arr[w], then w++. After loop fill rest with 0. Or: swap non-zero with position. O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Contains duplicate',
    syntax: 'function containsDuplicate(arr) {\n  return new Set(arr).size !== arr.length;\n}',
    description: 'Return true if any value appears at least twice.',
    returnType: 'boolean',
    details: 'Set stores unique; if size < length then duplicate. Sort and adjacent check O(n log n). Hash count O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Bit Manipulation',
    item: 'Single number (XOR)',
    syntax: 'function singleNumber(arr) {\n  return arr.reduce((a, b) => a ^ b, 0);\n}',
    description: 'Every element appears twice except one; find the single one. Use XOR.',
    returnType: 'number',
    details: 'a ^ a = 0, a ^ 0 = a. XOR all numbers; pairs cancel, result is the single. O(n) time, O(1) space.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Intersection of two arrays',
    syntax: 'function intersection(arr1, arr2) {\n  const set = new Set(arr1);\n  return [...new Set(arr2.filter(x => set.has(x)))];\n}',
    description: 'Return elements that appear in both arrays (unique).',
    returnType: 'array',
    details: 'Put one array in Set. Filter second by set.has(x), then unique with Set. Or count with object for intersection of duplicates.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Rotate array right by k',
    syntax: 'function rotateRight(arr, k) {\n  k = k % arr.length;\n  const reverse = (a, start, end) => {\n    while (start < end) [a[start], a[end]] = [a[end], a[start]], start++, end--;\n  };\n  reverse(arr, 0, arr.length - 1);\n  reverse(arr, 0, k - 1);\n  reverse(arr, k, arr.length - 1);\n  return arr;\n}',
    description: 'Rotate array to the right by k steps (in place or new array).',
    returnType: 'array',
    details: 'Reverse whole, then reverse [0, k) and [k, n] separately. Or: splice last k and unshift. k %= n to handle k > n.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Maximum subarray sum (Kadane)',
    syntax: 'function maxSubarraySum(arr) {\n  let max = arr[0], cur = arr[0];\n  for (let i = 1; i < arr.length; i++) {\n    cur = Math.max(arr[i], cur + arr[i]);\n    max = Math.max(max, cur);\n  }\n  return max;\n}',
    description: 'Find largest sum of any contiguous subarray.',
    returnType: 'number',
    details: 'Kadane: at each step either extend (cur + arr[i]) or start fresh (arr[i]). Track max. O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Greedy',
    item: 'Best time to buy and sell stock',
    syntax: 'function maxProfit(prices) {\n  let min = prices[0], maxProfit = 0;\n  for (let i = 1; i < prices.length; i++) {\n    maxProfit = Math.max(maxProfit, prices[i] - min);\n    min = Math.min(min, prices[i]);\n  }\n  return maxProfit;\n}',
    description: 'One buy, one sell; return max profit (prices[i] = price on day i).',
    returnType: 'number',
    details: 'Track min price so far. At each day profit = price - min; update maxProfit. O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'First unique character',
    syntax: 'function firstUniqChar(str) {\n  for (let i = 0; i < str.length; i++)\n    if (str.indexOf(str[i]) === str.lastIndexOf(str[i])) return i;\n  return -1;\n}',
    description: 'Return index of first non-repeating character, or -1.',
    returnType: 'number',
    details: 'indexOf === lastIndexOf means only one occurrence. Or: count chars in object, then loop to find count 1. O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Implement indexOf (strStr)',
    syntax: 'function strStr(haystack, needle) {\n  for (let i = 0; i <= haystack.length - needle.length; i++)\n    if (haystack.slice(i, i + needle.length) === needle) return i;\n  return -1;\n}',
    description: 'Return start index of needle in haystack, or -1.',
    returnType: 'number',
    details: 'Slide window: check haystack.slice(i, i+needle.length) === needle. Built-in: haystack.indexOf(needle). Can use KMP for O(n+m).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Length of last word',
    syntax: 'function lengthOfLastWord(str) {\n  const trimmed = str.trim();\n  const lastSpace = trimmed.lastIndexOf(" ");\n  return lastSpace === -1 ? trimmed.length : trimmed.length - lastSpace - 1;\n}',
    description: 'Return length of last word in string (words separated by spaces).',
    returnType: 'number',
    details: 'Trim, then lastIndexOf(" "). If -1 whole string is one word. Else length - lastSpace - 1. Or split and pop length.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Plus one (digit array)',
    syntax: 'function plusOne(digits) {\n  for (let i = digits.length - 1; i >= 0; i--) {\n    digits[i]++;\n    if (digits[i] < 10) return digits;\n    digits[i] = 0;\n  }\n  return [1, ...digits];\n}',
    description: 'Array of digits representing integer; add 1 and return new digits.',
    returnType: 'array',
    details: 'Start from end. Add 1; if < 10 return. Else set 0 and carry. If all 9s return [1, ...zeros].',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Binary Search',
    item: 'Integer square root',
    syntax: 'function mySqrt(x) {\n  let lo = 0, hi = x;\n  while (lo <= hi) {\n    const mid = (lo + hi) >> 1;\n    if (mid * mid <= x) lo = mid + 1;\n    else hi = mid - 1;\n  }\n  return hi;\n}',
    description: 'Return floor(sqrt(x)) using binary search.',
    returnType: 'number',
    details: 'Binary search on [0, x]. mid*mid <= x → try right; else left. Return hi (last valid). Or use Math.floor(Math.sqrt(x)).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'DP',
    item: 'Climbing stairs',
    syntax: 'function climbStairs(n) {\n  if (n <= 1) return 1;\n  let a = 1, b = 1;\n  for (let i = 2; i <= n; i++) [a, b] = [b, a + b];\n  return b;\n}',
    description: 'n steps; each step you climb 1 or 2. Return number of distinct ways to reach top.',
    returnType: 'number',
    details: 'Same as Fibonacci: ways(n) = ways(n-1) + ways(n-2). Iterative O(n). Can do matrix exponentiation for O(log n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Majority element',
    syntax: 'function majorityElement(arr) {\n  let count = 0, cand = null;\n  for (const x of arr) {\n    if (count === 0) cand = x;\n    count += x === cand ? 1 : -1;\n  }\n  return cand;\n}',
    description: 'Element that appears more than n/2 times. Return it (assume it exists).',
    returnType: 'number (or type of element)',
    details: 'Boyer-Moore: cancel pairs; leftover is candidate. O(n) time O(1) space. Or count in object and find count > n/2.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Roman to integer',
    syntax: 'function romanToInt(s) {\n  const map = { I: 1, V: 5, X: 10, L: 50, C: 100, D: 500, M: 1000 };\n  let sum = 0;\n  for (let i = 0; i < s.length; i++)\n    sum += map[s[i]] < (map[s[i + 1]] ?? 0) ? -map[s[i]] : map[s[i]];\n  return sum;\n}',
    description: 'Convert Roman numeral string to integer.',
    returnType: 'number',
    details: 'If current value < next, subtract (e.g. IV = -1+5). Else add. Loop and use map for char value.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Integer to Roman',
    syntax: 'function intToRoman(n) {\n  const vals = [1000,"M",900,"CM",500,"D",400,"CD",100,"C",90,"XC",50,"L",40,"XL",10,"X",9,"IX",5,"V",4,"IV",1,"I"];\n  let r = "";\n  for (let i = 0; i < vals.length; i += 2) {\n    while (n >= vals[i]) { r += vals[i + 1]; n -= vals[i]; }\n  }\n  return r;\n}',
    description: 'Convert integer to Roman numeral string.',
    returnType: 'string',
    details: 'Greedy: list (value, symbol) descending. For each, while n >= value append symbol and subtract.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Isomorphic strings',
    syntax: 'function isIsomorphic(s, t) {\n  if (s.length !== t.length) return false;\n  const m1 = {}, m2 = {};\n  for (let i = 0; i < s.length; i++) {\n    if (m1[s[i]] !== m2[t[i]]) return false;\n    m1[s[i]] = m2[t[i]] = i;\n  }\n  return true;\n}',
    description: 'Two strings isomorphic if we can map chars of s to t 1:1 (same structure).',
    returnType: 'boolean',
    details: 'Map s[i] → t[i] and t[i] → s[i]. If same index for both, structure matches. Or compare first-occurrence encoding of both.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Reverse integer',
    syntax: 'function reverseInteger(x) {\n  const sign = x < 0 ? -1 : 1;\n  x = Math.abs(x);\n  let r = 0;\n  while (x) { r = r * 10 + x % 10; x = Math.floor(x / 10); }\n  const out = sign * r;\n  return out < -(2 ** 31) || out > 2 ** 31 - 1 ? 0 : out;\n}',
    description: 'Reverse digits of integer; return 0 if result overflows 32-bit signed.',
    returnType: 'number',
    details: 'Strip sign, build reverse with r = r*10 + x%10, x = floor(x/10). Apply sign and clamp to [-2^31, 2^31-1].',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Binary Search',
    item: 'Search insert position',
    syntax: 'function searchInsert(arr, target) {\n  let lo = 0, hi = arr.length;\n  while (lo < hi) {\n    const mid = (lo + hi) >>> 1;\n    if (arr[mid] < target) lo = mid + 1;\n    else hi = mid;\n  }\n  return lo;\n}',
    description: 'Sorted array; find index where target would be inserted (or exists).',
    returnType: 'number',
    details: 'Binary search: find first index where arr[i] >= target. Return lo. Same as lower_bound.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Remove element (in place)',
    syntax: 'function removeElement(arr, val) {\n  let w = 0;\n  for (let i = 0; i < arr.length; i++)\n    if (arr[i] !== val) arr[w++] = arr[i];\n  return w;\n}',
    description: 'Remove all instances of val in place; return new length.',
    returnType: 'number',
    details: 'Write pointer: copy only elements !== val. Same pattern as move zeros. O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Remove duplicates from sorted array (in place)',
    syntax: 'function removeDuplicatesSorted(arr) {\n  if (arr.length === 0) return 0;\n  let w = 1;\n  for (let i = 1; i < arr.length; i++)\n    if (arr[i] !== arr[w - 1]) arr[w++] = arr[i];\n  return w;\n}',
    description: 'Sorted array; keep unique values in place, return new length.',
    returnType: 'number',
    details: 'Write pointer w: only write when arr[i] !== arr[w-1]. O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Max consecutive ones',
    syntax: 'function findMaxConsecutiveOnes(arr) {\n  let max = 0, cur = 0;\n  for (const b of arr) {\n    cur = b ? cur + 1 : 0;\n    max = Math.max(max, cur);\n  }\n  return max;\n}',
    description: 'Binary array; return length of longest contiguous segment of 1s.',
    returnType: 'number',
    details: 'If 1 increment cur, else reset cur to 0. Track max. O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Find pivot index',
    syntax: 'function pivotIndex(arr) {\n  const total = arr.reduce((a, b) => a + b, 0);\n  let left = 0;\n  for (let i = 0; i < arr.length; i++) {\n    if (left === total - left - arr[i]) return i;\n    left += arr[i];\n  }\n  return -1;\n}',
    description: 'Return index where sum of elements to the left equals sum to the right.',
    returnType: 'number',
    details: 'Total sum S. At index i, left sum = L; right = S - L - arr[i]. L === S - L - arr[i] → 2*L + arr[i] === S.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Running sum of 1D array',
    syntax: 'function runningSum(arr) {\n  const out = [];\n  let s = 0;\n  for (const x of arr) { s += x; out.push(s); }\n  return out;\n}',
    description: 'out[i] = arr[0] + arr[1] + … + arr[i].',
    returnType: 'array',
    details: 'Prefix sum. One pass with accumulator. Useful for range-sum queries.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Two Pointers',
    item: 'Squares of sorted array',
    syntax: 'function sortedSquares(arr) {\n  const out = [];\n  let left = 0, right = arr.length - 1;\n  while (left <= right) {\n    if (Math.abs(arr[left]) >= Math.abs(arr[right])) {\n      out.unshift(arr[left] * arr[left]); left++;\n    } else {\n      out.unshift(arr[right] * arr[right]); right--;\n    }\n  }\n  return out;\n}',
    description: 'Array sorted in non-decreasing order; return squares of elements in non-decreasing order.',
    returnType: 'array',
    details: 'Largest square at ends. Left at 0, right at end. Compare abs values, push larger square, move that pointer. O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Second largest in array',
    syntax: 'function secondLargest(arr) {\n  let first = -Infinity, second = -Infinity;\n  for (const x of arr) {\n    if (x > first) { second = first; first = x; }\n    else if (x > second && x !== first) second = x;\n  }\n  return second;\n}',
    description: 'Return second largest distinct value (or -Infinity if not found).',
    returnType: 'number',
    details: 'Track first and second. Update both when new max; update only second when first > x > second. One pass O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Kth largest element',
    syntax: 'function findKthLargest(arr, k) {\n  arr.sort((a, b) => b - a);\n  return arr[k - 1];\n}\n// or Quickselect for O(n) average',
    description: 'Return the kth largest element (1-based: k=1 is max).',
    returnType: 'number (or element type)',
    details: 'Sort descending and take index k-1: O(n log n). Quickselect: partition like quicksort but recurse only on side containing k: O(n) average.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Count primes (Sieve of Eratosthenes)',
    syntax: 'function countPrimes(n) {\n  const isPrime = Array(n).fill(true);\n  isPrime[0] = isPrime[1] = false;\n  for (let i = 2; i * i <= n; i++)\n    if (isPrime[i]) for (let j = i * i; j < n; j += i) isPrime[j] = false;\n  return isPrime.filter(Boolean).length;\n}',
    description: 'Count primes strictly less than n.',
    returnType: 'number',
    details: 'Sieve: mark multiples of 2,3,5,… as not prime. Start inner loop at i*i. O(n log log n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Bit Manipulation',
    item: 'Power of two',
    syntax: 'function isPowerOfTwo(n) {\n  return n > 0 && (n & (n - 1)) === 0;\n}',
    description: 'Return true if n is a power of 2 (1, 2, 4, 8, …).',
    returnType: 'boolean',
    details: 'Powers of 2 have one bit set. n & (n-1) clears lowest set bit; if result 0 and n>0 then one bit. n must be positive.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Bit Manipulation',
    item: 'Number of 1 bits (Hamming weight)',
    syntax: 'function hammingWeight(n) {\n  let count = 0;\n  while (n) { count += n & 1; n = n >>> 1; }\n  return count;\n}',
    description: 'Count how many 1 bits in 32-bit unsigned integer.',
    returnType: 'number',
    details: 'While n: add n&1, then n >>> 1. Or use n & (n-1) to clear lowest 1 and count iterations. JS: use >>> for unsigned.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Interval',
    item: 'Merge overlapping intervals',
    syntax: 'function mergeIntervals(intervals) {\n  if (intervals.length === 0) return [];\n  intervals.sort((a, b) => a[0] - b[0]);\n  const out = [intervals[0]];\n  for (let i = 1; i < intervals.length; i++) {\n    const last = out[out.length - 1];\n    if (intervals[i][0] <= last[1])\n      last[1] = Math.max(last[1], intervals[i][1]);\n    else out.push(intervals[i]);\n  }\n  return out;\n}',
    description: 'Array of [start, end]; merge overlapping intervals and return non-overlapping.',
    returnType: 'array of intervals',
    details: 'Sort intervals by start. Iterate: if intervals[i][0] <= out[last][1] then merge (update end); else push new. O(n log n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Group anagrams',
    syntax: 'function groupAnagrams(strs) {\n  const map = {};\n  for (const s of strs) {\n    const key = [...s].sort().join("");\n    if (!map[key]) map[key] = [];\n    map[key].push(s);\n  }\n  return Object.values(map);\n}',
    description: 'Group strings that are anagrams of each other.',
    returnType: 'array of arrays',
    details: 'Key = sorted string (or character count string). Same key → same group. Object.values(map) gives groups. O(n * k log k) with sort.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Two Pointers',
    item: 'Two Sum II (sorted array)',
    syntax: 'function twoSumII(arr, target) {\n  let left = 0, right = arr.length - 1;\n  while (left < right) {\n    const sum = arr[left] + arr[right];\n    if (sum === target) return [left + 1, right + 1];\n    if (sum < target) left++; else right--;\n  }\n  return [];\n}',
    description: 'Sorted array; find two indices (1-based) that sum to target. Two pointers.',
    returnType: 'array of two indices',
    details: 'If sum too small, left++; too large, right--. O(n). No extra space vs hash map.',
  },

  // ========== Algorithm: 2D Grid / Matrix ==========
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Count islands (DFS/BFS)',
    syntax: 'function numIslands(grid) {\n  const rows = grid.length, cols = grid[0]?.length || 0;\n  const dirs = [[0,1],[1,0],[0,-1],[-1,0]];\n  function dfs(r, c) {\n    if (r < 0 || r >= rows || c < 0 || c >= cols || grid[r][c] !== "1") return;\n    grid[r][c] = "0";\n    for (const [dr, dc] of dirs) dfs(r + dr, c + dc);\n  }\n  let count = 0;\n  for (let r = 0; r < rows; r++)\n    for (let c = 0; c < cols; c++)\n      if (grid[r][c] === "1") { dfs(r, c); count++; }\n  return count;\n}',
    description: '2D grid of "0" and "1"; count number of connected regions of "1" (islands).',
    returnType: 'number',
    details: 'Double loop over cells. When grid[i][j] is "1", run DFS to mark entire island (flip to "0"), then increment count. O(rows*cols).',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Largest island',
    syntax: 'function maxAreaOfIsland(grid) {\n  const rows = grid.length, cols = grid[0]?.length || 0;\n  const dirs = [[0,1],[1,0],[0,-1],[-1,0]];\n  function dfs(r, c) {\n    if (r < 0 || r >= rows || c < 0 || c >= cols || grid[r][c] !== 1) return 0;\n    grid[r][c] = 0;\n    let size = 1;\n    for (const [dr, dc] of dirs) size += dfs(r + dr, c + dc);\n    return size;\n  }\n  let maxSize = 0;\n  for (let r = 0; r < rows; r++)\n    for (let c = 0; c < cols; c++)\n      if (grid[r][c] === 1) maxSize = Math.max(maxSize, dfs(r, c));\n  return maxSize;\n}',
    description: 'Grid of 0/1; return the size (cell count) of the largest island.',
    returnType: 'number',
    details: 'For each island DFS returns area. Compare with global max. Same traversal as count islands; just count cells per component.',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Flood fill',
    syntax: 'function floodFill(image, sr, sc, newColor) {\n  const oldColor = image[sr][sc];\n  if (oldColor === newColor) return image;\n  const rows = image.length, cols = image[0].length;\n  function fill(r, c) {\n    if (r < 0 || r >= rows || c < 0 || c >= cols || image[r][c] !== oldColor) return;\n    image[r][c] = newColor;\n    fill(r+1,c); fill(r-1,c); fill(r,c+1); fill(r,c-1);\n  }\n  fill(sr, sc);\n  return image;\n}',
    description: 'Replace all cells connected to (sr,sc) with same color by new color (like paint bucket).',
    returnType: 'array (mutate grid)',
    details: 'DFS from (sr,sc): if in bounds and color matches original, set new color and recurse to 4 neighbors. Handle newColor === oldColor to avoid infinite loop.',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Longest path in grid',
    syntax: 'function longestIncreasingPath(matrix) {\n  const rows = matrix.length, cols = matrix[0]?.length || 0;\n  const dirs = [[-1,0],[1,0],[0,-1],[0,1]];\n  const memo = Array(rows).fill(0).map(() => Array(cols).fill(0));\n  function dfs(r, c) {\n    if (memo[r][c]) return memo[r][c];\n    let max = 1;\n    for (const [dr, dc] of dirs) {\n      const nr = r + dr, nc = c + dc;\n      if (nr >= 0 && nr < rows && nc >= 0 && nc < cols && matrix[nr][nc] > matrix[r][c])\n        max = Math.max(max, 1 + dfs(nr, nc));\n    }\n    memo[r][c] = max;\n    return max;\n  }\n  let result = 0;\n  for (let r = 0; r < rows; r++)\n    for (let c = 0; c < cols; c++) result = Math.max(result, dfs(r, c));\n  return result;\n}',
    description: 'Find length of longest strictly increasing path in 2D grid.',
    returnType: 'number',
    details: 'DP/DFS with memo: longest path from (r,c) = 1 + max over valid neighbors. Valid = in bounds and value strictly greater.',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Grid traversal (up/down/left/right)',
    syntax: 'function traverseNeighbors(grid, r, c, visit) {\n  const rows = grid.length, cols = grid[0]?.length || 0;\n  const dirs = [[-1,0],[1,0],[0,-1],[0,1]];\n  for (const [dr, dc] of dirs) {\n    const nr = r + dr, nc = c + dc;\n    if (nr >= 0 && nr < rows && nc >= 0 && nc < cols)\n      visit(nr, nc, grid[nr][nc]);\n  }\n}',
    description: 'Standard 4-direction movement: row ±1, col ±1.',
    returnType: 'N/A',
    details: 'Offsets: (-1,0) up, (1,0) down, (0,-1) left, (0,1) right. Check bounds before accessing. Use for BFS/DFS in grid.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Graph',
    item: 'Count connected components',
    syntax: 'function countComponents(n, edges) {\n  const adj = Array(n).fill(0).map(() => []);\n  for (const [a, b] of edges) { adj[a].push(b); adj[b].push(a); }\n  const visited = new Set();\n  function dfs(node) {\n    if (visited.has(node)) return;\n    visited.add(node);\n    for (const next of adj[node]) dfs(next);\n  }\n  let count = 0;\n  for (let i = 0; i < n; i++)\n    if (!visited.has(i)) { dfs(i); count++; }\n  return count;\n}',
    description: 'In a graph (n nodes, edge list), count how many separate connected groups.',
    returnType: 'number',
    details: 'Build adjacency list from edges. For each unvisited node, run DFS and increment count. Same idea for 2D grid (use numIslands).',
  },

  // ========== Algorithm: Arrays (frequency, sliding window) ==========
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Frequency counting',
    syntax: 'function frequencyCount(arr) {\n  const freq = {};\n  for (const x of arr) freq[x] = (freq[x] ?? 0) + 1;\n  return freq;\n}',
    description: 'Count how many times each value appears (object or Map).',
    returnType: 'object or Map',
    details: 'Loop and increment freq[x]. Use for "find duplicate", "majority", "anagram". Map is better when keys are not strings.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Sliding Window',
    item: 'Sliding window (longest substring without repeating)',
    syntax: 'function lengthOfLongestSubstring(s) {\n  let left = 0, set = new Set(), max = 0;\n  for (let right = 0; right < s.length; right++) {\n    while (set.has(s[right])) set.delete(s[left++]);\n    set.add(s[right]);\n    max = Math.max(max, right - left + 1);\n  }\n  return max;\n}',
    description: 'Longest substring with all distinct characters. Expand right, shrink left when duplicate.',
    returnType: 'number',
    details: 'Window [left, right]. Add s[right]; if s[right] already in set, move left and remove s[left] until no duplicate. Track max length. O(n).',
  },

  // ========== Algorithm: Strings (template, validation, char freq) ==========
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Template placeholder replacement',
    syntax: 'function replacePlaceholders(str, data) {\n  return str.replace(/\\{\\{(\w+)\\}\\}/g, (_, key) => data[key] ?? "");\n}',
    description: 'Replace {{name}} or {{key}} in string with values from an object.',
    returnType: 'string',
    details: 'Regex /\\{\\{(\w+)\\}\\}/g matches {{key}}; replace callback uses data[key]. Escape braces in regex.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Email / input validation',
    syntax: 'function isValidEmail(str) {\n  return /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(str);\n}\nfunction isValidInput(str, minLen, pattern) {\n  return str.length >= minLen && pattern.test(str);\n}',
    description: 'Basic email format or generic input validation with regex.',
    returnType: 'boolean',
    details: 'Email: non-whitespace, @, domain, dot, TLD. Use regex or built-in type="email" + server-side check.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Character frequency (string)',
    syntax: 'function charFrequency(str) {\n  const freq = {};\n  for (const c of str) freq[c] = (freq[c] ?? 0) + 1;\n  return freq;\n}',
    description: 'Count occurrence of each character in a string.',
    returnType: 'object (or Map)',
    details: 'Same pattern as array frequency counting. Use for anagram, first unique, compression. Map works for Unicode.',
  },

  // ========== Algorithm: Recursion / Backtracking ==========
  {
    category: 'Algorithm',
    algorithmType: 'Recursion',
    item: 'Traverse nested JSON',
    syntax: 'function traverse(obj) {\n  if (typeof obj !== "object" || obj === null) return;\n  for (const key in obj) { console.log(key, obj[key]); traverse(obj[key]); }\n}',
    description: 'Recursively visit every key and value in nested objects/arrays.',
    returnType: 'N/A (side effect)',
    details: 'Base: primitive or null → return. Recurse: for each key (or index), process value and call traverse(value). Use to search, collect, or transform.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Recursion',
    item: 'Count total nodes in tree-like structure',
    syntax: 'function countNodes(node) {\n  if (!node) return 0;\n  return 1 + (node.children?.reduce((s, c) => s + countNodes(c), 0) ?? 0);\n}',
    description: 'Tree or nested structure: count all nodes (e.g. node + children).',
    returnType: 'number',
    details: 'Base: null/undefined → 0. Else 1 + sum of countNodes(child) for each child. For binary tree: 1 + countNodes(left) + countNodes(right).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Backtracking',
    item: 'Generate subsets',
    syntax: 'function subsets(arr, i = 0, path = [], out = []) {\n  if (i === arr.length) { out.push([...path]); return; }\n  path.push(arr[i]); subsets(arr, i+1, path, out); path.pop();\n  subsets(arr, i+1, path, out);\n  return out;\n}',
    description: 'Generate all subsets (power set) of an array. Include or skip each element.',
    returnType: 'array of arrays',
    details: 'At each index: either include arr[i] (push, recurse, pop) or skip. When i === length, push copy of path. 2^n subsets.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Backtracking',
    item: 'Generate permutations',
    syntax: 'function permute(arr, start = 0, out = []) {\n  if (start === arr.length) { out.push([...arr]); return; }\n  for (let i = start; i < arr.length; i++) {\n    [arr[start], arr[i]] = [arr[i], arr[start]];\n    permute(arr, start + 1, out);\n    [arr[start], arr[i]] = [arr[i], arr[start]];\n  }\n  return out;\n}',
    description: 'Generate all permutations of array (swap-based backtracking).',
    returnType: 'array of arrays',
    details: 'Fix position start; try each arr[i] at start (swap start with i), recurse start+1, swap back. When start === length, push copy. n! permutations.',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Path finding in grid (DFS/BFS)',
    syntax: 'function shortestPathGrid(grid, start, end) {\n  const rows = grid.length, cols = grid[0]?.length || 0;\n  const dirs = [[-1,0],[1,0],[0,-1],[0,1]];\n  const queue = [[start[0], start[1], 0]], visited = new Set([`${start[0]},${start[1]}`]);\n  while (queue.length) {\n    const [r, c, dist] = queue.shift();\n    if (r === end[0] && c === end[1]) return dist;\n    for (const [dr, dc] of dirs) {\n      const nr = r + dr, nc = c + dc, key = `${nr},${nc}`;\n      if (nr >= 0 && nr < rows && nc >= 0 && nc < cols && !visited.has(key) && grid[nr][nc] !== 1) {\n        visited.add(key);\n        queue.push([nr, nc, dist + 1]);\n      }\n    }\n  }\n  return -1;\n}',
    description: 'Shortest path length from start to end in 2D grid; 1 = wall, 0 = open. BFS.',
    returnType: 'number (distance or -1)',
    details: 'BFS explores by layers; first time we reach end is shortest. Queue holds [r, c, distance]. grid[nr][nc] === 1 means wall.',
  },

  // ========== Algorithm: Sorting ==========
  {
    category: 'Algorithm',
    algorithmType: 'Sorting',
    item: 'Custom sort objects',
    syntax: 'function sortByAge(arr) {\n  return arr.sort((a, b) => a.age - b.age);\n}\nfunction sortByName(arr) {\n  return arr.sort((a, b) => a.name.localeCompare(b.name));\n}',
    description: 'Sort array of objects by a property (number or string).',
    returnType: 'same array (mutated)',
    details: 'compareFn: return negative if a before b, positive if a after b, 0 if equal. Numbers: a.prop - b.prop. Strings: localeCompare.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Sorting',
    item: 'Sort by frequency',
    syntax: 'function sortByFrequency(arr) {\n  const freq = {};\n  arr.forEach(x => freq[x] = (freq[x] ?? 0) + 1);\n  return arr.sort((a, b) => freq[b] - freq[a] || a - b);\n}',
    description: 'Sort elements by how often they appear (most frequent first); same freq then ascending value.',
    returnType: 'array',
    details: 'Count frequency. Sort by freq[b]-freq[a] for descending count. Tie-break with value: || a-b.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Sorting',
    item: 'Multi-field sorting',
    syntax: 'function sortByMultiple(arr) {\n  return arr.sort((a, b) => {\n    if (a.x !== b.x) return a.x - b.x;\n    if (a.y !== b.y) return a.y - b.y;\n    return a.z - b.z;\n  });\n}',
    description: 'Sort by first key; if equal, by second key; then third, etc.',
    returnType: 'same array (mutated)',
    details: 'Compare first field; if !== return comparison. Else compare next field. For strings use localeCompare.',
  },

  // ========== Algorithm: Graph-like ==========
  {
    category: 'Algorithm',
    algorithmType: 'Graph',
    item: 'Adjacency list traversal',
    syntax: 'function dfs(adj, node, visited = new Set()) {\n  if (visited.has(node)) return;\n  visited.add(node);\n  for (const n of adj[node] ?? []) dfs(adj, n, visited);\n}\nfunction bfs(adj, start) {\n  const queue = [start], visited = new Set([start]);\n  while (queue.length) {\n    const node = queue.shift();\n    for (const n of adj[node] ?? [])\n      if (!visited.has(n)) { visited.add(n); queue.push(n); }\n  }\n  return visited;\n}',
    description: 'Traverse graph represented as object: node → list of neighbors. DFS and BFS.',
    returnType: 'N/A (side effect)',
    details: 'Build adj from edges: for each [u,v] add v to adj[u] and u to adj[v] if undirected.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Graph',
    item: 'Friend connection problems',
    syntax: 'function countFriendGroups(n, friendships) {\n  const adj = Array(n).fill(0).map(() => []);\n  for (const [a, b] of friendships) { adj[a].push(b); adj[b].push(a); }\n  const visited = new Set();\n  function dfs(node) {\n    if (visited.has(node)) return;\n    visited.add(node);\n    for (const next of adj[node]) dfs(next);\n  }\n  let count = 0;\n  for (let i = 0; i < n; i++) { if (!visited.has(i)) { dfs(i); count++; } }\n  return count;\n}\nfunction areConnected(adj, A, B) {\n  const visited = new Set();\n  const queue = [A]; visited.add(A);\n  while (queue.length) {\n    const node = queue.shift();\n    if (node === B) return true;\n    for (const n of adj[node] ?? []) if (!visited.has(n)) { visited.add(n); queue.push(n); }\n  }\n  return false;\n}',
    description: 'Count friend groups (connected components) or check if two people are connected.',
    returnType: 'number or boolean',
    details: 'countFriendGroups: same as countComponents. areConnected: BFS from A, return true if B is visited.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Graph',
    item: 'Shortest connection (basic BFS)',
    syntax: 'function shortestPath(adj, start, end) {\n  const queue = [[start, 0]], visited = new Set([start]);\n  while (queue.length) {\n    const [node, dist] = queue.shift();\n    if (node === end) return dist;\n    for (const n of adj[node] ?? []) {\n      if (!visited.has(n)) {\n        visited.add(n);\n        queue.push([n, dist + 1]);\n      }\n    }\n  }\n  return -1;\n}',
    description: 'Unweighted graph: shortest path length from start to end (number of edges). BFS.',
    returnType: 'number (distance or -1)',
    details: 'BFS explores by layers; first time we reach end is shortest. For weighted edges use Dijkstra.',
  },

  // ========== Algorithm: 50 more ==========
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Product of array except self',
    syntax: 'function productExceptSelf(nums) {\n  const n = nums.length;\n  const out = Array(n).fill(1);\n  let left = 1;\n  for (let i = 0; i < n; i++) { out[i] = left; left *= nums[i]; }\n  let right = 1;\n  for (let i = n - 1; i >= 0; i--) { out[i] *= right; right *= nums[i]; }\n  return out;\n}',
    description: 'Return array where out[i] = product of all elements except nums[i]. O(n), no division.',
    returnType: 'array',
    details: 'Two passes: left products then multiply by right products. Avoid division to handle zeros.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Two Pointers',
    item: 'Trapping rain water',
    syntax: 'function trap(height) {\n  let left = 0, right = height.length - 1, maxL = 0, maxR = 0, water = 0;\n  while (left < right) {\n    if (height[left] <= height[right]) {\n      if (height[left] >= maxL) maxL = height[left]; else water += maxL - height[left];\n      left++;\n    } else {\n      if (height[right] >= maxR) maxR = height[right]; else water += maxR - height[right];\n      right--;\n    }\n  }\n  return water;\n}',
    description: 'Elevation map: compute total water trapped after rain (two pointers).',
    returnType: 'number',
    details: 'At each index water += min(maxLeft, maxRight) - height[i]. Two-pointer avoids extra arrays.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Two Pointers',
    item: 'Container with most water',
    syntax: 'function maxArea(height) {\n  let left = 0, right = height.length - 1, maxA = 0;\n  while (left < right) {\n    maxA = Math.max(maxA, (right - left) * Math.min(height[left], height[right]));\n    if (height[left] < height[right]) left++; else right--;\n  }\n  return maxA;\n}',
    description: 'Lines at indices i with height[i]; find two lines that form container with most water.',
    returnType: 'number',
    details: 'Area = (right - left) * min(height[left], height[right]). Move pointer at shorter line.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Next permutation',
    syntax: 'function nextPermutation(nums) {\n  let i = nums.length - 2;\n  while (i >= 0 && nums[i] >= nums[i + 1]) i--;\n  if (i >= 0) {\n    let j = nums.length - 1;\n    while (nums[j] <= nums[i]) j--;\n    [nums[i], nums[j]] = [nums[j], nums[i]];\n  }\n  let l = i + 1, r = nums.length - 1;\n  while (l < r) [nums[l], nums[r]] = [nums[r], nums[l]], l++, r--;\n  return nums;\n}',
    description: 'Rearrange into next lexicographically greater permutation; in place.',
    returnType: 'same array (mutated)',
    details: 'Find first dip from right (nums[i] < nums[i+1]); swap with smallest greater to the right; reverse right part.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Greedy',
    item: 'Jump game',
    syntax: 'function canJump(nums) {\n  let maxReach = 0;\n  for (let i = 0; i < nums.length; i++) {\n    if (i > maxReach) return false;\n    maxReach = Math.max(maxReach, i + nums[i]);\n    if (maxReach >= nums.length - 1) return true;\n  }\n  return true;\n}',
    description: 'nums[i] = max jump from index i; return true if you can reach last index.',
    returnType: 'boolean',
    details: 'Track furthest index reachable. If i > maxReach, stuck. O(n).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Greedy',
    item: 'Jump game II',
    syntax: 'function jump(nums) {\n  let jumps = 0, curEnd = 0, farthest = 0;\n  for (let i = 0; i < nums.length - 1; i++) {\n    farthest = Math.max(farthest, i + nums[i]);\n    if (i === curEnd) { jumps++; curEnd = farthest; }\n  }\n  return jumps;\n}',
    description: 'Minimum number of jumps to reach last index (assume always reachable).',
    returnType: 'number',
    details: 'BFS-like: when we reach curEnd, take a jump and extend curEnd to farthest.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Interval',
    item: 'Insert interval',
    syntax: 'function insertInterval(intervals, newInterval) {\n  const out = [];\n  let i = 0;\n  while (i < intervals.length && intervals[i][1] < newInterval[0]) out.push(intervals[i++]);\n  while (i < intervals.length && intervals[i][0] <= newInterval[1]) {\n    newInterval[0] = Math.min(newInterval[0], intervals[i][0]);\n    newInterval[1] = Math.max(newInterval[1], intervals[i][1]);\n    i++;\n  }\n  out.push(newInterval);\n  while (i < intervals.length) out.push(intervals[i++]);\n  return out;\n}',
    description: 'Non-overlapping intervals sorted by start; insert newInterval and merge if needed.',
    returnType: 'array of intervals',
    details: 'Add all ending before newInterval; merge overlapping; add newInterval; add rest.',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Unique paths (m x n grid)',
    syntax: 'function uniquePaths(m, n) {\n  const dp = Array(m).fill(0).map(() => Array(n).fill(1));\n  for (let r = 1; r < m; r++)\n    for (let c = 1; c < n; c++) dp[r][c] = dp[r-1][c] + dp[r][c-1];\n  return dp[m-1][n-1];\n}',
    description: 'Robot at top-left, move only right or down; count paths to bottom-right.',
    returnType: 'number',
    details: 'DP: paths(r,c) = paths(r-1,c) + paths(r,c-1). Can optimize to 1D array.',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Min path sum (grid)',
    syntax: 'function minPathSum(grid) {\n  const rows = grid.length, cols = grid[0].length;\n  for (let r = 0; r < rows; r++)\n    for (let c = 0; c < cols; c++) {\n      if (r === 0 && c === 0) continue;\n      const fromTop = r > 0 ? grid[r-1][c] : Infinity;\n      const fromLeft = c > 0 ? grid[r][c-1] : Infinity;\n      grid[r][c] += Math.min(fromTop, fromLeft);\n    }\n  return grid[rows-1][cols-1];\n}',
    description: 'Grid of numbers; find minimum sum path from top-left to bottom-right (move right/down).',
    returnType: 'number',
    details: 'DP in place: grid[r][c] += min(grid[r-1][c], grid[r][c-1]).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'DP',
    item: 'House robber',
    syntax: 'function rob(nums) {\n  let prev = 0, curr = 0;\n  for (const x of nums) {\n    const next = Math.max(curr, prev + x);\n    prev = curr;\n    curr = next;\n  }\n  return curr;\n}',
    description: 'Max sum of non-adjacent elements (cannot rob two adjacent houses).',
    returnType: 'number',
    details: 'DP: rob up to i = max(rob up to i-1, rob up to i-2 + nums[i]). O(n) space O(1).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'DP',
    item: 'House robber II (circular)',
    syntax: 'function robCircle(nums) {\n  if (nums.length === 1) return nums[0];\n  function robRange(arr) {\n    let prev = 0, curr = 0;\n    for (const x of arr) { const next = Math.max(curr, prev + x); prev = curr; curr = next; }\n    return curr;\n  }\n  return Math.max(robRange(nums.slice(0, -1)), robRange(nums.slice(1)));\n}',
    description: 'Same as house robber but houses are in a circle (first and last adjacent).',
    returnType: 'number',
    details: 'Max of: rob 0..n-2 and rob 1..n-1 (exclude one of first/last).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'DP',
    item: 'Coin change',
    syntax: 'function coinChange(coins, amount) {\n  const dp = Array(amount + 1).fill(Infinity);\n  dp[0] = 0;\n  for (let a = 1; a <= amount; a++)\n    for (const c of coins)\n      if (c <= a) dp[a] = Math.min(dp[a], 1 + dp[a - c]);\n  return dp[amount] === Infinity ? -1 : dp[amount];\n}',
    description: 'Minimum number of coins to make amount; return -1 if impossible.',
    returnType: 'number',
    details: 'DP: dp[a] = 1 + min(dp[a - c]) over coins c. Unbounded knapsack style.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'DP',
    item: 'Longest increasing subsequence (LIS)',
    syntax: 'function lengthOfLIS(nums) {\n  if (nums.length === 0) return 0;\n  const dp = Array(nums.length).fill(1);\n  for (let i = 1; i < nums.length; i++)\n    for (let j = 0; j < i; j++)\n      if (nums[j] < nums[i]) dp[i] = Math.max(dp[i], dp[j] + 1);\n  return Math.max(...dp);\n}',
    description: 'Length of longest strictly increasing subsequence (not necessarily contiguous).',
    returnType: 'number',
    details: 'DP: dp[i] = 1 + max dp[j] for j < i and nums[j] < nums[i]. O(n²); binary search version O(n log n).',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Word search (2D grid)',
    syntax: 'function exist(board, word) {\n  const rows = board.length, cols = board[0].length;\n  const dirs = [[-1,0],[1,0],[0,-1],[0,1]];\n  function dfs(r, c, i) {\n    if (i === word.length) return true;\n    if (r < 0 || r >= rows || c < 0 || c >= cols || board[r][c] !== word[i]) return false;\n    const ch = board[r][c]; board[r][c] = "";\n    for (const [dr, dc] of dirs) if (dfs(r+dr, c+dc, i+1)) return true;\n    board[r][c] = ch;\n    return false;\n  }\n  for (let r = 0; r < rows; r++)\n    for (let c = 0; c < cols; c++) if (dfs(r, c, 0)) return true;\n  return false;\n}',
    description: 'Find if word exists in grid (adjacent cells, each cell used once).',
    returnType: 'boolean',
    details: 'DFS from each cell; mark visited by clearing cell; backtrack restore.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Backtracking',
    item: 'Subsets II (with duplicates)',
    syntax: 'function subsetsWithDup(nums) {\n  nums.sort((a, b) => a - b);\n  const out = [];\n  function backtrack(start, path) {\n    out.push([...path]);\n    for (let i = start; i < nums.length; i++) {\n      if (i > start && nums[i] === nums[i-1]) continue;\n      path.push(nums[i]);\n      backtrack(i + 1, path);\n      path.pop();\n    }\n  }\n  backtrack(0, []);\n  return out;\n}',
    description: 'All subsets of array (may have duplicates); avoid duplicate subsets.',
    returnType: 'array of arrays',
    details: 'Sort first. Skip same value at same level: if (i > start && nums[i] === nums[i-1]) continue.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Backtracking',
    item: 'Combination sum',
    syntax: 'function combinationSum(candidates, target) {\n  const out = [];\n  function backtrack(start, path, sum) {\n    if (sum === target) { out.push([...path]); return; }\n    if (sum > target) return;\n    for (let i = start; i < candidates.length; i++) {\n      path.push(candidates[i]);\n      backtrack(i, path, sum + candidates[i]);\n      path.pop();\n    }\n  }\n  backtrack(0, [], 0);\n  return out;\n}',
    description: 'All unique combinations where candidate numbers sum to target (same number may be used repeatedly).',
    returnType: 'array of arrays',
    details: 'Backtrack; reuse same index (i not i+1) for repeat use.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Backtracking',
    item: 'Combination sum II',
    syntax: 'function combinationSum2(candidates, target) {\n  candidates.sort((a, b) => a - b);\n  const out = [];\n  function backtrack(start, path, sum) {\n    if (sum === target) { out.push([...path]); return; }\n    if (sum > target) return;\n    for (let i = start; i < candidates.length; i++) {\n      if (i > start && candidates[i] === candidates[i-1]) continue;\n      path.push(candidates[i]);\n      backtrack(i + 1, path, sum + candidates[i]);\n      path.pop();\n    }\n  }\n  backtrack(0, [], 0);\n  return out;\n}',
    description: 'Combinations that sum to target; each candidate used once; no duplicate combinations.',
    returnType: 'array of arrays',
    details: 'Sort; skip duplicate at same level. Use i+1 so each number used once.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Backtracking',
    item: 'Letter combinations of phone number',
    syntax: 'function letterCombinations(digits) {\n  if (digits.length === 0) return [];\n  const map = { "2":"abc","3":"def","4":"ghi","5":"jkl","6":"mno","7":"pqrs","8":"tuv","9":"wxyz" };\n  const out = [];\n  function backtrack(i, path) {\n    if (i === digits.length) { out.push(path.join("")); return; }\n    for (const c of map[digits[i]] ?? "") {\n      path.push(c);\n      backtrack(i + 1, path);\n      path.pop();\n    }\n  }\n  backtrack(0, []);\n  return out;\n}',
    description: 'All letter combinations that digits (2-9) could represent (phone keypad).',
    returnType: 'array of strings',
    details: 'Backtrack over each digit; try each letter for that digit.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Backtracking',
    item: 'Generate parentheses',
    syntax: 'function generateParenthesis(n) {\n  const out = [];\n  function backtrack(open, close, path) {\n    if (path.length === 2 * n) { out.push(path.join("")); return; }\n    if (open < n) { path.push("("); backtrack(open + 1, close, path); path.pop(); }\n    if (close < open) { path.push(")"); backtrack(open, close + 1, path); path.pop(); }\n  }\n  backtrack(0, 0, []);\n  return out;\n}',
    description: 'Generate all valid combinations of n pairs of parentheses.',
    returnType: 'array of strings',
    details: 'Only add ")" when close < open. Track open and close counts.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'DP',
    item: 'Word break',
    syntax: 'function wordBreak(s, wordDict) {\n  const set = new Set(wordDict);\n  const dp = Array(s.length + 1).fill(false);\n  dp[0] = true;\n  for (let i = 1; i <= s.length; i++)\n    for (let j = 0; j < i; j++)\n      if (dp[j] && set.has(s.slice(j, i))) { dp[i] = true; break; }\n  return dp[s.length];\n}',
    description: 'Return true if s can be segmented into space-separated words from wordDict.',
    returnType: 'boolean',
    details: 'DP: dp[i] = true if s[0..i) can be segmented. Check all splits j < i.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Valid palindrome II (one deletion)',
    syntax: 'function validPalindrome(s) {\n  function isPal(l, r) {\n    while (l < r) { if (s[l] !== s[r]) return false; l++; r--; }\n    return true;\n  }\n  let l = 0, r = s.length - 1;\n  while (l < r) {\n    if (s[l] !== s[r]) return isPal(l+1, r) || isPal(l, r-1);\n    l++; r--;\n  }\n  return true;\n}',
    description: 'Return true if s is palindrome or can be one after removing at most one character.',
    returnType: 'boolean',
    details: 'When mismatch at l,r try skip left (l+1,r) or skip right (l,r-1).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Longest palindromic substring',
    syntax: 'function longestPalindrome(s) {\n  function expand(l, r) {\n    while (l >= 0 && r < s.length && s[l] === s[r]) l--, r++;\n    return s.slice(l + 1, r);\n  }\n  let best = "";\n  for (let i = 0; i < s.length; i++) {\n    const odd = expand(i, i), even = expand(i, i + 1);\n    if (odd.length > best.length) best = odd;\n    if (even.length > best.length) best = even;\n  }\n  return best;\n}',
    description: 'Return longest substring that is a palindrome (expand around center).',
    returnType: 'string',
    details: 'For each center (single char or between two), expand while equal. O(n²).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Zigzag conversion',
    syntax: 'function convert(s, numRows) {\n  if (numRows === 1) return s;\n  const rows = Array(numRows).fill("");\n  let r = 0, step = 1;\n  for (const c of s) {\n    rows[r] += c;\n    r += step;\n    if (r === 0 || r === numRows - 1) step = -step;\n  }\n  return rows.join("");\n}',
    description: 'Write string in zigzag pattern over numRows rows; return row-by-row string.',
    returnType: 'string',
    details: 'Simulate: move row index up/down; flip direction at top and bottom.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'String compression',
    syntax: 'function compress(chars) {\n  let w = 0, i = 0;\n  while (i < chars.length) {\n    const c = chars[i];\n    let count = 0;\n    while (i < chars.length && chars[i] === c) { count++; i++; }\n    chars[w++] = c;\n    if (count > 1) for (const d of String(count)) chars[w++] = d;\n  }\n  return w;\n}',
    description: 'In-place: replace consecutive repeated chars with char + count (e.g. aabbccc → a2b2c3). Return new length.',
    returnType: 'number',
    details: 'Two pointers: read run length, write char and digits. Count > 1 only then write digits.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Ransom note',
    syntax: 'function canConstruct(ransomNote, magazine) {\n  const freq = {};\n  for (const c of magazine) freq[c] = (freq[c] ?? 0) + 1;\n  for (const c of ransomNote) {\n    if (!freq[c]) return false;\n    freq[c]--;\n  }\n  return true;\n}',
    description: 'Return true if ransomNote can be formed from characters in magazine.',
    returnType: 'boolean',
    details: 'Count magazine chars; decrement for each ransom char. If any go negative, false.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'First letter to appear twice',
    syntax: 'function repeatedCharacter(s) {\n  const set = new Set();\n  for (const c of s) {\n    if (set.has(c)) return c;\n    set.add(c);\n  }\n  return "";\n}',
    description: 'Return the first character that appears twice (left to right).',
    returnType: 'string',
    details: 'Track seen in Set; return on first repeat.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Check if sentence is pangram',
    syntax: 'function checkIfPangram(sentence) {\n  const set = new Set(sentence.toLowerCase().replace(/[^a-z]/g, ""));\n  return set.size === 26;\n}',
    description: 'Return true if sentence contains every letter a-z at least once.',
    returnType: 'boolean',
    details: 'Add each letter to Set; check size 26. Or use bitmask for a-z.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Find the difference (added letter)',
    syntax: 'function findTheDifference(s, t) {\n  let xor = 0;\n  for (const c of s) xor ^= c.charCodeAt(0);\n  for (const c of t) xor ^= c.charCodeAt(0);\n  return String.fromCharCode(xor);\n}',
    description: 't is s with one letter added at random position; return the added letter.',
    returnType: 'string',
    details: 'XOR all char codes; pairs cancel, leftover is the extra char.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Add binary',
    syntax: 'function addBinary(a, b) {\n  let carry = 0, out = "";\n  let i = a.length - 1, j = b.length - 1;\n  while (i >= 0 || j >= 0 || carry) {\n    const sum = (i >= 0 ? +a[i--] : 0) + (j >= 0 ? +b[j--] : 0) + carry;\n    out = (sum % 2) + out;\n    carry = sum >> 1;\n  }\n  return out;\n}',
    description: 'Two binary strings; return their sum as binary string.',
    returnType: 'string',
    details: 'Add from right to left; carry = sum / 2, digit = sum % 2.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Multiply strings',
    syntax: 'function multiply(num1, num2) {\n  if (num1 === "0" || num2 === "0") return "0";\n  const m = num1.length, n = num2.length;\n  const arr = Array(m + n).fill(0);\n  for (let i = m - 1; i >= 0; i--)\n    for (let j = n - 1; j >= 0; j--) {\n      const p = num1[i] * num2[j] + arr[i + j + 1];\n      arr[i + j + 1] = p % 10;\n      arr[i + j] += Math.floor(p / 10);\n    }\n  return arr.join("").replace(/^0+/, "") || "0";\n}',
    description: 'Multiply two non-negative integers given as strings; return product as string.',
    returnType: 'string',
    details: 'Digit multiply; store in array by position; handle carry. Trim leading zeros.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'String',
    item: 'Simplify path (Unix)',
    syntax: 'function simplifyPath(path) {\n  const stack = [];\n  for (const seg of path.split("/")) {\n    if (seg === "..") stack.pop();\n    else if (seg && seg !== ".") stack.push(seg);\n  }\n  return "/" + stack.join("/");\n}',
    description: 'Simplify Unix-style path: resolve "..", ".", and redundant slashes.',
    returnType: 'string',
    details: 'Split by "/"; ".." pop, "." skip, else push. Join with "/".',
  },
  {
    category: 'Algorithm',
    algorithmType: 'DP',
    item: 'Decode ways',
    syntax: 'function numDecodings(s) {\n  const dp = { [s.length]: 1 };\n  for (let i = s.length - 1; i >= 0; i--) {\n    if (s[i] === "0") { dp[i] = 0; continue; }\n    dp[i] = dp[i + 1];\n    if (i + 1 < s.length && parseInt(s.slice(i, i + 2)) <= 26) dp[i] += dp[i + 2];\n  }\n  return dp[0];\n}',
    description: 'Digits 1-26 map to A-Z; return number of ways to decode the string.',
    returnType: 'number',
    details: 'DP: decode from i = decode from i+1 (one digit) + decode from i+2 if two digits valid.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'DP',
    item: 'Min cost climbing stairs',
    syntax: 'function minCostClimbingStairs(cost) {\n  let prev = 0, curr = 0;\n  for (let i = 2; i <= cost.length; i++)\n    [prev, curr] = [curr, Math.min(prev + cost[i-2], curr + cost[i-1])];\n  return curr;\n}',
    description: 'cost[i] = cost to step on i; start at index 0 or 1, climb to past end. Min total cost.',
    returnType: 'number',
    details: 'DP: minCost to reach i = cost[i] + min(minCost(i-1), minCost(i-2)).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Maximum product subarray',
    syntax: 'function maxProduct(nums) {\n  let maxP = nums[0], maxCur = nums[0], minCur = nums[0];\n  for (let i = 1; i < nums.length; i++) {\n    const x = nums[i];\n    const nextMax = Math.max(x, maxCur * x, minCur * x);\n    const nextMin = Math.min(x, maxCur * x, minCur * x);\n    maxCur = nextMax; minCur = nextMin;\n    maxP = Math.max(maxP, maxCur);\n  }\n  return maxP;\n}',
    description: 'Largest product of contiguous subarray (handle negative × negative).',
    returnType: 'number',
    details: 'Track max and min product ending at i (min for negative flip).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Binary Search',
    item: 'Find minimum in rotated sorted array',
    syntax: 'function findMinRotated(nums) {\n  let left = 0, right = nums.length - 1;\n  while (left < right) {\n    const mid = (left + right) >> 1;\n    if (nums[mid] > nums[right]) left = mid + 1;\n    else right = mid;\n  }\n  return nums[left];\n}',
    description: 'Sorted array rotated; find minimum. No duplicates assumed.',
    returnType: 'number',
    details: 'Binary search: if nums[mid] > nums[right], min in right half; else left half including mid.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Binary Search',
    item: 'Search in rotated sorted array',
    syntax: 'function searchRotated(nums, target) {\n  let left = 0, right = nums.length - 1;\n  while (left <= right) {\n    const mid = (left + right) >> 1;\n    if (nums[mid] === target) return mid;\n    if (nums[left] <= nums[mid]) {\n      if (target >= nums[left] && target < nums[mid]) right = mid - 1;\n      else left = mid + 1;\n    } else {\n      if (target > nums[mid] && target <= nums[right]) left = mid + 1;\n      else right = mid - 1;\n    }\n  }\n  return -1;\n}',
    description: 'Sorted array rotated; search target. Return index or -1.',
    returnType: 'number',
    details: 'Binary search; one half is always sorted. Check which half contains target.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Binary Search',
    item: 'Find peak element',
    syntax: 'function findPeakElement(nums) {\n  let left = 0, right = nums.length - 1;\n  while (left < right) {\n    const mid = (left + right) >> 1;\n    if (nums[mid] < nums[mid + 1]) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}',
    description: 'Peak = element greater than neighbors; find any peak index (nums[-1] = nums[n] = -∞).',
    returnType: 'number',
    details: 'Binary search: if nums[mid] < nums[mid+1], peak in right half; else left half.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Binary Search',
    item: 'Search 2D matrix',
    syntax: 'function searchMatrix(matrix, target) {\n  const rows = matrix.length, cols = matrix[0]?.length || 0;\n  let left = 0, right = rows * cols - 1;\n  while (left <= right) {\n    const mid = (left + right) >> 1;\n    const r = Math.floor(mid / cols), c = mid % cols;\n    const v = matrix[r][c];\n    if (v === target) return true;\n    if (v < target) left = mid + 1; else right = mid - 1;\n  }\n  return false;\n}',
    description: 'Matrix sorted row-wise and row first < next row first; search target.',
    returnType: 'boolean',
    details: 'Flatten to 1D index: mid → (mid/cols, mid%cols). Binary search.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Binary Search',
    item: 'Find first and last position (sorted)',
    syntax: 'function searchRange(nums, target) {\n  function findFirst() {\n    let lo = 0, hi = nums.length;\n    while (lo < hi) {\n      const mid = (lo + hi) >> 1;\n      if (nums[mid] < target) lo = mid + 1; else hi = mid;\n    }\n    return lo;\n  }\n  function findLast() {\n    let lo = 0, hi = nums.length;\n    while (lo < hi) {\n      const mid = (lo + hi) >> 1;\n      if (nums[mid] <= target) lo = mid + 1; else hi = mid;\n    }\n    return lo - 1;\n  }\n  const left = findFirst();\n  if (left === nums.length || nums[left] !== target) return [-1, -1];\n  return [left, findLast()];\n}',
    description: 'Sorted array; find start and end index of target; return [-1,-1] if not found.',
    returnType: 'array of two numbers',
    details: 'Binary search for first >= target and last <= target.',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Valid Sudoku',
    syntax: 'function isValidSudoku(board) {\n  const rows = Array(9).fill(0).map(() => ({}));\n  const cols = Array(9).fill(0).map(() => ({}));\n  const boxes = Array(9).fill(0).map(() => ({}));\n  for (let r = 0; r < 9; r++)\n    for (let c = 0; c < 9; c++) {\n      const ch = board[r][c];\n      if (ch === ".") continue;\n      const b = 3 * Math.floor(r/3) + Math.floor(c/3);\n      if (rows[r][ch] || cols[c][ch] || boxes[b][ch]) return false;\n      rows[r][ch] = cols[c][ch] = boxes[b][ch] = true;\n    }\n  return true;\n}',
    description: 'Check if 9x9 Sudoku board is valid (no duplicate in row, column, 3x3 box).',
    returnType: 'boolean',
    details: 'Track seen in row r, col c, box 3*(r/3)+c/3. Ignore ".".',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Spiral matrix',
    syntax: 'function spiralOrder(matrix) {\n  if (matrix.length === 0) return [];\n  const out = [];\n  let top = 0, bottom = matrix.length - 1, left = 0, right = matrix[0].length - 1;\n  while (top <= bottom && left <= right) {\n    for (let c = left; c <= right; c++) out.push(matrix[top][c]); top++;\n    for (let r = top; r <= bottom; r++) out.push(matrix[r][right]); right--;\n    if (top <= bottom) for (let c = right; c >= left; c--) out.push(matrix[bottom][c]); bottom--;\n    if (left <= right) for (let r = bottom; r >= top; r--) out.push(matrix[r][left]); left++;\n  }\n  return out;\n}',
    description: 'Return elements of matrix in spiral order (right, down, left, up).',
    returnType: 'array',
    details: 'Four loops per layer; shrink top, right, bottom, left. Check bounds for inner two.',
  },
  {
    category: 'Algorithm',
    algorithmType: '2D Grid',
    item: 'Set matrix zeroes',
    syntax: 'function setZeroes(matrix) {\n  const rows = matrix.length, cols = matrix[0].length;\n  let col0 = false;\n  for (let r = 0; r < rows; r++) {\n    if (matrix[r][0] === 0) col0 = true;\n    for (let c = 1; c < cols; c++)\n      if (matrix[r][c] === 0) matrix[r][0] = matrix[0][c] = 0;\n  }\n  for (let r = rows - 1; r >= 0; r--) {\n    for (let c = cols - 1; c >= 1; c--)\n      if (matrix[r][0] === 0 || matrix[0][c] === 0) matrix[r][c] = 0;\n    if (col0) matrix[r][0] = 0;\n  }\n  return matrix;\n}',
    description: 'If an element is 0, set its entire row and column to 0. In place.',
    returnType: 'same matrix (mutated)',
    details: 'Use first row and first column as markers; col0 flag for column 0.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Find all numbers disappeared in array',
    syntax: 'function findDisappearedNumbers(nums) {\n  const n = nums.length;\n  for (let i = 0; i < n; i++) {\n    const idx = Math.abs(nums[i]) - 1;\n    if (nums[idx] > 0) nums[idx] = -nums[idx];\n  }\n  const out = [];\n  for (let i = 0; i < n; i++) if (nums[i] > 0) out.push(i + 1);\n  return out;\n}',
    description: 'Array of n integers in [1,n]; some appear once, others twice. Return numbers that do not appear.',
    returnType: 'array',
    details: 'Mark index (nums[i]-1) as seen by negating. Indices still positive are missing.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Array',
    item: 'Third maximum number',
    syntax: 'function thirdMax(nums) {\n  let first = -Infinity, second = -Infinity, third = -Infinity;\n  for (const x of new Set(nums)) {\n    if (x > first) { third = second; second = first; first = x; }\n    else if (x > second) { third = second; second = x; }\n    else if (x > third) third = x;\n  }\n  return third === -Infinity ? first : third;\n}',
    description: 'Return third distinct maximum; if fewer than three distinct, return maximum.',
    returnType: 'number',
    details: 'Track first, second, third. Use Set for distinct. Return first if no third.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Add digits (digital root)',
    syntax: 'function addDigits(num) {\n  return num === 0 ? 0 : (num % 9 === 0 ? 9 : num % 9);\n}\n// or loop: while (num >= 10) num = [...String(num)].reduce((s, d) => s + +d, 0); return num;',
    description: 'Repeatedly add digits until single digit (digital root).',
    returnType: 'number',
    details: 'Math: digital root of n is 1 + (n-1) % 9, except 0 → 0.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Happy number',
    syntax: 'function isHappy(n) {\n  const seen = new Set();\n  while (n !== 1 && !seen.has(n)) {\n    seen.add(n);\n    n = String(n).split("").reduce((s, d) => s + d * d, 0);\n  }\n  return n === 1;\n}',
    description: 'Replace n by sum of squares of digits; return true if eventually 1 (happy).',
    returnType: 'boolean',
    details: 'Loop until 1 or cycle (use Set). Sum of squares of digits each step.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Ugly number',
    syntax: 'function isUgly(n) {\n  if (n <= 0) return false;\n  for (const p of [2, 3, 5]) while (n % p === 0) n /= p;\n  return n === 1;\n}',
    description: 'Ugly = prime factors only 2, 3, 5. Return true if n is ugly.',
    returnType: 'boolean',
    details: 'Divide by 2, 3, 5 until possible; remainder should be 1.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Excel sheet column number',
    syntax: 'function titleToNumber(columnTitle) {\n  let n = 0;\n  for (const c of columnTitle) n = n * 26 + (c.charCodeAt(0) - 64);\n  return n;\n}',
    description: 'Convert column title (A, B, ..., Z, AA, ...) to number (1-indexed).',
    returnType: 'number',
    details: 'Base-26: A=1,...,Z=26. n = n*26 + (char - 64).',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Excel sheet column title',
    syntax: 'function numberToTitle(n) {\n  let s = "";\n  while (n > 0) {\n    n--;\n    s = String.fromCharCode(65 + n % 26) + s;\n    n = Math.floor(n / 26);\n  }\n  return s;\n}',
    description: 'Convert number to Excel column title (1→A, 27→AA).',
    returnType: 'string',
    details: 'Base-26 with 1-indexed: n-- then s = chr(65 + n%26) + s, n = n/26.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Factorial trailing zeroes',
    syntax: 'function trailingZeroes(n) {\n  let count = 0;\n  while (n >= 5) { n = Math.floor(n / 5); count += n; }\n  return count;\n}',
    description: 'Return number of trailing zeroes in n! (count factors of 5).',
    returnType: 'number',
    details: 'Trailing zeroes from 10 = 2*5; more 5s than 2s in n!. Count = n/5 + n/25 + n/125 + ...',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Perfect square',
    syntax: 'function isPerfectSquare(n) {\n  if (n < 0) return false;\n  const x = Math.floor(Math.sqrt(n));\n  return x * x === n;\n}',
    description: 'Return true if n is a perfect square (without using sqrt library: binary search).',
    returnType: 'boolean',
    details: 'Binary search for x where x*x === n. Or use Math.sqrt and check integer.',
  },
  {
    category: 'Algorithm',
    algorithmType: 'Math',
    item: 'Sqrt (Newton\'s method)',
    syntax: 'function mySqrtNewton(x) {\n  if (x <= 1) return x;\n  let guess = x >> 1;\n  while (guess > x / guess) guess = (guess + x / guess) >> 1;\n  return Math.floor(guess);\n}',
    description: 'Integer square root using Newton\'s method (iterative).',
    returnType: 'number',
    details: 'Update guess = (guess + x/guess) / 2 until convergence. Return floor.',
  },

  // ========== HTML (with tag for drill-down) ==========
  { category: 'HTML', tag: 'form', item: 'Form action', syntax: '<form action="url">', description: 'URL where form data is submitted.', returnType: 'N/A', details: 'action attribute: the endpoint or page that receives the form submission (e.g. /submit or script URL).' },
  { category: 'HTML', tag: 'form', item: 'Form method GET vs POST', syntax: '<form method="GET">  |  <form method="POST">', description: 'HTTP method used to submit the form.', returnType: 'N/A', details: 'GET: data in URL query string (visible, bookmarkable). POST: data in request body (not in URL, for sensitive or large data).' },
  { category: 'HTML', tag: 'form', item: 'Form name', syntax: 'name="fieldName"', description: 'Name of the form or form control; used when submitting data.', returnType: 'N/A', details: 'name is sent with form data (key in key-value pairs). Required for form controls to be submitted.' },
  { category: 'HTML', tag: 'form', item: 'Form enctype', syntax: '<form enctype="multipart/form-data">', description: 'Encoding type for form submission.', returnType: 'N/A', details: 'enctype: application/x-www-form-urlencoded (default), multipart/form-data (file uploads), text/plain.' },
  { category: 'HTML', tag: 'form', item: 'Form target', syntax: '<form target="_blank">', description: 'Where to open response: _self, _blank, _parent, _top, or frame name.', returnType: 'N/A', details: 'target="_blank" opens response in new tab/window.' },
  { category: 'HTML', tag: 'form', item: 'label', syntax: '<label for="id">Text</label>', description: 'Associates label with form control; clicking label focuses control.', returnType: 'N/A', details: 'for attribute matches id of input. Improves accessibility and UX.' },
  { category: 'HTML', tag: 'form', item: 'fieldset and legend', syntax: '<fieldset><legend>Title</legend>...</fieldset>', description: 'Group related form controls with a caption.', returnType: 'N/A', details: 'fieldset groups inputs; legend is the caption. Good for accessibility and styling.' },
  { category: 'HTML', tag: 'form', item: 'textarea', syntax: '<textarea name="msg" rows="4" cols="40">Default</textarea>', description: 'Multi-line text input. Sent to Express: string.', returnType: 'N/A', details: 'Attributes: name, rows, cols, placeholder, required, readonly, disabled, maxlength, minlength. Content is initial value; submitted as name=value. If not sent or empty: req.body.name is "" (empty string) when field is present but empty; undefined if field is missing or disabled.' },

  { category: 'HTML', tag: 'input', item: 'id attribute', syntax: 'id="uniqueId"', description: 'Unique identifier for an element.', returnType: 'N/A', details: 'id must be unique in the document. Used for linking (anchor), CSS, and JavaScript getElementById.' },
  { category: 'HTML', tag: 'input', item: 'value attribute', syntax: 'value="initialValue"', description: 'Initial or current value of an input/option/button.', returnType: 'N/A', details: 'value is what gets submitted for that control and what is shown/used as the control\'s value.' },
  { category: 'HTML', tag: 'input', item: 'placeholder', syntax: 'placeholder="hint text"', description: 'Hint shown when input is empty.', returnType: 'N/A', details: 'placeholder is not submitted; it is only a visual hint. Do not use as replacement for <label>.' },
  { category: 'HTML', tag: 'input', item: 'required', syntax: 'required', description: 'Form cannot submit until this field has a value.', returnType: 'N/A', details: 'required is a boolean attribute. Browser validates before submit. Use for mandatory fields.' },
  { category: 'HTML', tag: 'input', item: 'readonly', syntax: 'readonly', description: 'User cannot edit the value; still submitted.', returnType: 'N/A', details: 'readonly: value is visible and submitted but not editable. Unlike disabled, readonly fields are included in form data.' },
  { category: 'HTML', tag: 'input', item: 'disabled', syntax: 'disabled', description: 'Control is disabled; not submitted with form.', returnType: 'N/A', details: 'disabled excludes the field from form submission. Value is not sent.' },
  { category: 'HTML', tag: 'input', item: 'Input type text', syntax: '<input type="text">', description: 'Single-line text input. Sent to Express: string.', returnType: 'N/A', details: 'Default type. type="text" for plain single-line text. If not sent or empty: req.body.name is "" when field is present but empty; undefined if field is missing or disabled.' },
  { category: 'HTML', tag: 'input', item: 'Input type radio', syntax: '<input type="radio" name="group">', description: 'Radio button; only one in a group can be selected. Sent to Express: string (selected value).', returnType: 'N/A', details: 'Radio buttons are grouped by name. Same name = one selection per group. value is what gets submitted. If not sent: req.body.name is undefined when no radio in the group is selected (name is omitted from submission).' },
  { category: 'HTML', tag: 'input', item: 'Input type checkbox', syntax: '<input type="checkbox">', description: 'Checkbox for on/off or multiple selections. Sent to Express: string (single checked) or array (multiple same name, with extended: true).', returnType: 'N/A', details: 'Checked checkboxes submit their name and value (or "on" if no value). Unchecked boxes are not submitted. If not sent: req.body.name is undefined when checkbox is unchecked or when no checkbox with that name is checked.' },
  { category: 'HTML', tag: 'input', item: 'Input type submit', syntax: '<input type="submit">', description: 'Button that submits the form. Sent to Express: string (name=value of clicked button, only if name is set).', returnType: 'N/A', details: 'type="submit" sends the form. Value attribute becomes the button label. If not sent: req.body.name is undefined for a submit button that was not clicked (only the clicked submit with a name appears in body).' },
  { category: 'HTML', tag: 'input', item: 'Input type hidden', syntax: '<input type="hidden" name="x" value="y">', description: 'Invisible field; value is submitted with form. Sent to Express: string.', returnType: 'N/A', details: 'hidden inputs are not visible. Use for tokens, IDs, or extra data that must be sent with the form. If not sent: req.body.name is undefined only if the hidden field is missing from the form or disabled; when present it is always sent (string, possibly "").' },
  { category: 'HTML', tag: 'input', item: 'Input type password', syntax: '<input type="password">', description: 'Text input with characters masked. Sent to Express: string.', returnType: 'N/A', details: 'type="password" masks input. Value is still submitted as plain text; encrypt on server if needed. If not sent or empty: req.body.name is "" when field is present but empty; undefined if field is missing or disabled.' },
  { category: 'HTML', tag: 'input', item: 'Input type email', syntax: '<input type="email">', description: 'Email input; browser may validate format. Sent to Express: string (or array if multiple attribute).', returnType: 'N/A', details: 'type="email" can trigger keyboard and validation. Still submit to server for real validation. If not sent or empty: req.body.name is "" when field is present but empty; undefined if field is missing or disabled.' },
  { category: 'HTML', tag: 'input', item: 'Input type number', syntax: '<input type="number" min="0" max="100">', description: 'Numeric input; optional min, max, step. Sent to Express: string.', returnType: 'N/A', details: 'type="number" restricts to numbers. min, max, step for range. value is string in form data. If not sent or empty: req.body.name is "" when field is present but empty; undefined if field is missing or disabled.' },
  { category: 'HTML', tag: 'input', item: 'Input type file', syntax: '<input type="file" accept=".pdf">', description: 'File upload. accept limits file types. Sent to Express: file(s) via multipart (e.g. req.files with multer), not in req.body as string.', returnType: 'N/A', details: 'type="file" for uploads. Use enctype="multipart/form-data" on form. accept hints allowed types. If no file chosen: req.files may be undefined or [] depending on multer; file inputs are not in req.body.' },
  { category: 'HTML', tag: 'input', item: 'Input type button', syntax: '<input type="button" value="Click">', description: 'Button with no default behavior. Sent to Express: not submitted.', returnType: 'N/A', details: 'type="button" does not submit form. Use for custom JS actions. Prefer <button type="button">. req.body.name is always undefined (button is never submitted).' },
  { category: 'HTML', tag: 'input', item: 'Input type reset', syntax: '<input type="reset">', description: 'Resets form controls to initial values. Sent to Express: not submitted.', returnType: 'N/A', details: 'type="reset" resets all fields in the form to their initial state when clicked. req.body.name is always undefined (reset is never submitted).' },
  { category: 'HTML', tag: 'input', item: 'autofocus', syntax: 'autofocus', description: 'Focus this control when page loads.', returnType: 'N/A', details: 'Only one autofocus per page. Use sparingly for accessibility. Used on: text, number, email, password, and most input types.' },
  { category: 'HTML', tag: 'input', item: 'maxlength', syntax: 'maxlength="100"', description: 'Maximum number of characters allowed.', returnType: 'N/A', details: 'maxlength prevents typing more than the given number. Used on: text, email, password, search, tel, url. Always validate on server.' },
  { category: 'HTML', tag: 'input', item: 'minlength', syntax: 'minlength="2"', description: 'Minimum number of characters required.', returnType: 'N/A', details: 'minlength validates at least N characters. Used on: text, email, password, search, tel, url.' },
  { category: 'HTML', tag: 'input', item: 'size', syntax: 'size="20"', description: 'Visual width of input in characters (approximate).', returnType: 'N/A', details: 'size hints display width; use CSS for precise control. Used on: text, email, password, search, tel, url.' },
  { category: 'HTML', tag: 'input', item: 'step', syntax: 'step="0.1"  or  step="any"', description: 'Increment for number/range/date inputs.', returnType: 'N/A', details: 'step constrains valid values. Used on: number, range, date, datetime-local, time.' },
  { category: 'HTML', tag: 'input', item: 'min and max', syntax: 'min="0" max="100"', description: 'Minimum and maximum value for number/range/date.', returnType: 'N/A', details: 'min/max validate range. Used on: number, range, date, datetime-local, time, month, week.' },
  { category: 'HTML', tag: 'input', item: 'accept', syntax: 'accept=".pdf,image/*"', description: 'Hint for allowed file types (file input).', returnType: 'N/A', details: 'accept limits file picker; not enforced. Used on: type="file". Comma-separated MIME types or extensions.' },
  { category: 'HTML', tag: 'input', item: 'checked', syntax: 'checked  or  checked="checked"', description: 'Pre-select radio or checkbox.', returnType: 'N/A', details: 'Boolean attribute. Used on: type="radio", type="checkbox". One radio per name can be checked.' },
  { category: 'HTML', tag: 'input', item: 'Attributes for type=text', syntax: '<input type="text">', description: 'Attributes available for text input. Sent to Express: string.', returnType: 'N/A', details: 'For type="text": id, name, value, placeholder, required, readonly, disabled, maxlength, minlength, size, autofocus, list, pattern, title, class. If empty or missing: req.body.name is "" or undefined.' },
  { category: 'HTML', tag: 'input', item: 'Attributes for type=number', syntax: '<input type="number">', description: 'Attributes available for number input. Sent to Express: string.', returnType: 'N/A', details: 'For type="number": id, name, value, min, max, step, required, readonly, disabled, placeholder, autofocus, list, class. If empty or missing: req.body.name is "" or undefined.' },
  { category: 'HTML', tag: 'input', item: 'Attributes for type=radio', syntax: '<input type="radio" name="group">', description: 'Attributes available for radio input. Sent to Express: string.', returnType: 'N/A', details: 'For type="radio": name (required for grouping), value, checked, required, disabled, autofocus. Same name = one selection per group. If no selection: req.body.name is undefined.' },
  { category: 'HTML', tag: 'input', item: 'Attributes for type=checkbox', syntax: '<input type="checkbox">', description: 'Attributes available for checkbox input. Sent to Express: string or array.', returnType: 'N/A', details: 'For type="checkbox": name, value, checked, required, disabled, autofocus. Unchecked boxes are not submitted. If none checked: req.body.name is undefined.' },
  { category: 'HTML', tag: 'input', item: 'Attributes for type=email', syntax: '<input type="email">', description: 'Attributes available for email input. Sent to Express: string or array.', returnType: 'N/A', details: 'For type="email": id, name, value, placeholder, required, readonly, disabled, maxlength, minlength, size, autofocus, multiple (for multiple emails), pattern, list. If empty or missing: req.body.name is "" or undefined.' },
  { category: 'HTML', tag: 'input', item: 'Attributes for type=password', syntax: '<input type="password">', description: 'Attributes available for password input. Sent to Express: string.', returnType: 'N/A', details: 'For type="password": id, name, value, placeholder, required, readonly, disabled, maxlength, minlength, size, autofocus, pattern, title. If empty or missing: req.body.name is "" or undefined.' },
  { category: 'HTML', tag: 'input', item: 'Attributes for type=file', syntax: '<input type="file">', description: 'Attributes available for file input. Sent to Express: file(s) via multipart (e.g. req.files).', returnType: 'N/A', details: 'For type="file": name, accept, multiple, required, disabled. Form must use enctype="multipart/form-data". If no file: req.files undefined or []; not in req.body.' },
  { category: 'HTML', tag: 'input', item: 'Attributes for type=submit', syntax: '<input type="submit" value="Send">', description: 'Attributes available for submit button. Sent to Express: string (if name set).', returnType: 'N/A', details: 'For type="submit": value (button label), name (sent with form if present), disabled, form (id of form to submit). If that submit not clicked: req.body.name is undefined.' },
  { category: 'HTML', tag: 'input', item: 'Attributes for type=hidden', syntax: '<input type="hidden" name="x" value="y">', description: 'Attributes for hidden input. Sent to Express: string.', returnType: 'N/A', details: 'For type="hidden": name, value (required). Used for tokens, IDs, or extra data submitted with form. If missing/disabled: undefined; when present always string.' },

  { category: 'HTML', tag: 'select', item: 'select element', syntax: '<select name="choice"><option>...</option></select>', description: 'Dropdown or list of options. Sent to Express: string (selected option value).', returnType: 'N/A', details: 'select creates a dropdown. name is submitted; value comes from selected option. Use multiple for multi-select. size shows N lines. If not sent or empty: req.body.name is "" when an option with no value or empty value is selected; undefined if select is missing or disabled.' },
  { category: 'HTML', tag: 'select', item: 'option', syntax: '<option value="a">Label A</option>', description: 'One option in a select.', returnType: 'N/A', details: 'value is what gets submitted; text content is label. selected preselects. Use optgroup to group options.' },
  { category: 'HTML', tag: 'select', item: 'optgroup', syntax: '<optgroup label="Group name"><option>...</option></optgroup>', description: 'Group options in a select.', returnType: 'N/A', details: 'optgroup groups option elements. label is the group heading (not selectable).' },
  { category: 'HTML', tag: 'select', item: 'select multiple', syntax: '<select name="choices" multiple>', description: 'Allow multiple selections. Sent to Express: array (with express.urlencoded({ extended: true })).', returnType: 'N/A', details: 'multiple attribute allows selecting more than one option. Submitted value is multiple name=values (depends on server). If none selected: req.body.name is undefined (name is omitted from submission).' },
  { category: 'HTML', tag: 'select', item: 'select size', syntax: 'size="4"', description: 'Number of visible options (height of list).', returnType: 'N/A', details: 'size makes select show as list box with N visible rows. Without size it is a dropdown.' },
  { category: 'HTML', tag: 'select', item: 'selected (option)', syntax: '<option value="x" selected>', description: 'Pre-select an option.', returnType: 'N/A', details: 'selected boolean attribute on option. One selected per select (or multiple if select has multiple).' },

  { category: 'HTML', tag: 'table', item: 'Table colspan', syntax: '<td colspan="2">', description: 'Cell spans multiple columns.', returnType: 'N/A', details: 'colspan merges cells horizontally. Number is how many columns the cell spans.' },
  { category: 'HTML', tag: 'table', item: 'Table rowspan', syntax: '<td rowspan="2">', description: 'Cell spans multiple rows.', returnType: 'N/A', details: 'rowspan merges cells vertically. Number is how many rows the cell spans.' },
  { category: 'HTML', tag: 'table', item: 'thead', syntax: '<thead><tr><th>...</th></tr></thead>', description: 'Table header section.', returnType: 'N/A', details: 'thead groups header rows. Use <th> for header cells. Semantic and can be styled separately.' },
  { category: 'HTML', tag: 'table', item: 'tbody', syntax: '<tbody><tr><td>...</td></tr></tbody>', description: 'Table body section.', returnType: 'N/A', details: 'tbody groups data rows. Optional but good for structure and styling.' },
  { category: 'HTML', tag: 'table', item: 'tfoot', syntax: '<tfoot><tr><td>...</td></tr></tfoot>', description: 'Table footer section (e.g. totals).', returnType: 'N/A', details: 'tfoot for footer rows. Renders after tbody in some browsers; semantic for summary rows.' },
  { category: 'HTML', tag: 'table', item: 'table structure', syntax: '<table><tr><th>H</th></tr><tr><td>D</td></tr></table>', description: 'table contains tr (rows), tr contains th/td (cells).', returnType: 'N/A', details: 'table > tr > th or td. th for headers (bold by default), td for data. Use thead/tbody/tfoot for structure.' },
  { category: 'HTML', tag: 'table', item: 'caption', syntax: '<table><caption>Title</caption>...</table>', description: 'Table title/caption.', returnType: 'N/A', details: 'caption should be first or last child of table. Describes the table for accessibility.' },

  { category: 'HTML', tag: 'paths', item: 'Relative path ./', syntax: 'href="./file.html"', description: 'Current directory.', returnType: 'N/A', details: './ means current directory. ./file.html is file in same folder as the current page.' },
  { category: 'HTML', tag: 'paths', item: 'Relative path ../', syntax: 'href="../file.html"', description: 'Parent directory.', returnType: 'N/A', details: '../ means parent directory. ../file.html goes up one level then to file.html.' },
  { category: 'HTML', tag: 'paths', item: 'Root path /', syntax: 'href="/file.html"', description: 'Root of site or server.', returnType: 'N/A', details: '/ at start means root. /file.html is at the root of the domain or app.' },
  { category: 'HTML', tag: 'paths', item: 'Absolute URL', syntax: 'href="https://example.com/page"', description: 'Full URL to another site or resource.', returnType: 'N/A', details: 'Full URL includes protocol. Use for external links. Consider target="_blank" and rel="noopener" for external.' },
  { category: 'HTML', tag: 'paths', item: 'Anchor in page', syntax: 'id="section1"  and  href="#section1"', description: 'Link to a specific element on the same page.', returnType: 'N/A', details: 'Give element an id, then link with href="#id". Browser scrolls to that element.' },

  { category: 'HTML', tag: 'attributes', item: 'class', syntax: 'class="box primary"', description: 'Space-separated list of CSS classes.', returnType: 'N/A', details: 'class can have multiple values. Used for styling and JS selection. Not unique.' },
  { category: 'HTML', tag: 'attributes', item: 'data-* attributes', syntax: 'data-id="123"', description: 'Custom data attributes for JS; no semantic meaning.', returnType: 'N/A', details: 'data-* attributes are valid HTML5. Access in JS via element.dataset.id (data-id).' },
  { category: 'HTML', tag: 'attributes', item: 'title', syntax: 'title="tooltip text"', description: 'Tooltip shown on hover.', returnType: 'N/A', details: 'title shows as native tooltip. Do not rely for critical info; not available on keyboard-only or touch.' },
  { category: 'HTML', tag: 'attributes', item: 'tabindex', syntax: 'tabindex="0"  or  tabindex="-1"', description: 'Control tab order and focusability.', returnType: 'N/A', details: 'tabindex="0" includes in natural order; -1 allows programmatic focus but not tab. Positive values avoid.' },

  { category: 'HTML', tag: 'a', item: 'anchor (link)', syntax: '<a href="url">text</a>', description: 'Hyperlink to URL or in-page anchor.', returnType: 'N/A', details: 'href is destination (URL or #id). Click navigates. Use target and rel for external links.' },
  { category: 'HTML', tag: 'a', item: 'href attribute', syntax: 'href="url"  or  href="#section"', description: 'Link destination URL or fragment.', returnType: 'N/A', details: 'Absolute: https://... Relative: ./page, ../page. Fragment: #id for same-page anchor. Omit for placeholder link.' },
  { category: 'HTML', tag: 'a', item: 'target attribute', syntax: 'target="_blank"  |  target="_self"', description: 'Where to open the link.', returnType: 'N/A', details: '_blank = new tab/window. _self = same (default). Use rel="noopener noreferrer" with _blank for security.' },
  { category: 'HTML', tag: 'a', item: 'rel attribute (links)', syntax: 'rel="noopener noreferrer"', description: 'Relationship / security for links.', returnType: 'N/A', details: 'noopener prevents new page from accessing window.opener. noreferrer hides referrer. Use with target="_blank".' },
  { category: 'HTML', tag: 'a', item: 'download attribute', syntax: 'download  or  download="filename"', description: 'Hint to download linked resource instead of navigate.', returnType: 'N/A', details: 'download triggers download; optional value suggests filename. Same-origin only for custom filename.' },

  { category: 'HTML', tag: 'img', item: 'img element', syntax: '<img src="url" alt="description">', description: 'Embed an image.', returnType: 'N/A', details: 'src = image URL (required). alt = text alternative (required for accessibility).' },
  { category: 'HTML', tag: 'img', item: 'img src and alt', syntax: '<img src="photo.jpg" alt="A cat">', description: 'Image source and alternative text.', returnType: 'N/A', details: 'src can be relative (./img/photo.jpg) or absolute. alt shown if image fails or for screen readers; keep descriptive.' },
  { category: 'HTML', tag: 'img', item: 'img width and height', syntax: 'width="200" height="100"', description: 'Image dimensions (optional; use CSS for layout).', returnType: 'N/A', details: 'width/height in pixels. Setting both reserves space and reduces layout shift (CLS). Prefer CSS for responsive.' },
  { category: 'HTML', tag: 'img', item: 'img loading', syntax: 'loading="lazy"', description: 'Defer loading off-screen images.', returnType: 'N/A', details: 'loading="lazy" loads image when near viewport. loading="eager" (default) loads immediately. Helps performance.' },

  { category: 'HTML', tag: 'div', item: 'div element', syntax: '<div>...</div>', description: 'Block-level generic container.', returnType: 'N/A', details: 'div has no semantic meaning. Use for layout/grouping when no semantic tag fits. Block by default.' },
  { category: 'HTML', tag: 'span', item: 'span element', syntax: '<span>...</span>', description: 'Inline generic container.', returnType: 'N/A', details: 'span has no semantic meaning. Use to wrap inline content for styling or JS. Inline by default.' },
  { category: 'HTML', tag: 'div', item: 'div vs span', syntax: 'div = block  |  span = inline', description: 'div is block; span is inline.', returnType: 'N/A', details: 'div starts on new line and takes full width. span stays inline with text. Use for grouping without semantics.' },

  { category: 'HTML', tag: 'list', item: 'ul unordered list', syntax: '<ul><li>item</li></ul>', description: 'Bulleted list.', returnType: 'N/A', details: 'ul = unordered list. li = list item. Use for bullet points. Nest ul/ol inside li for sublists.' },
  { category: 'HTML', tag: 'list', item: 'ol ordered list', syntax: '<ol><li>first</li><li>second</li></ol>', description: 'Numbered list.', returnType: 'N/A', details: 'ol = ordered list. li = list item. Numbers auto-increment. Use start attribute to change start number.' },
  { category: 'HTML', tag: 'list', item: 'li list item', syntax: '<li>content</li>', description: 'One item in ul or ol.', returnType: 'N/A', details: 'li must be direct child of ul, ol, or menu. Can contain block or inline content, including nested lists.' },
  { category: 'HTML', tag: 'list', item: 'ol start and type', syntax: 'start="5"  type="A"', description: 'Ordered list start number and numbering style.', returnType: 'N/A', details: 'start = first number. type = 1 (numbers), A/a (letters), I/i (Roman). CSS list-style-type preferred for styling.' },

  { category: 'HTML', tag: 'button', item: 'button element', syntax: '<button type="submit">Send</button>', description: 'Clickable button; can contain content.', returnType: 'N/A', details: 'type: submit (submit form), button (no default), reset (reset form). Prefer over input type=submit for styling.' },
  { category: 'HTML', tag: 'button', item: 'button type', syntax: 'type="submit"  |  type="button"  |  type="reset"', description: 'Button behavior.', returnType: 'N/A', details: 'submit = submit form. button = no default (use for JS). reset = reset form fields. Default in form is submit.' },
  { category: 'HTML', tag: 'button', item: 'button vs input', syntax: '<button>Label</button>  vs  <input type="submit" value="Label">', description: 'button can wrap content; input cannot.', returnType: 'N/A', details: 'button can contain HTML (e.g. icon + text). input value is plain text only. Use button for flexible styling.' },

  { category: 'HTML', tag: 'semantic', item: 'header', syntax: '<header>...</header>', description: 'Intro or nav block for page/section.', returnType: 'N/A', details: 'header typically holds logo, nav, or section title. Can be used multiple times (e.g. per article).' },
  { category: 'HTML', tag: 'semantic', item: 'footer', syntax: '<footer>...</footer>', description: 'Footer for page or section.', returnType: 'N/A', details: 'footer typically holds copyright, links, meta. Can be used multiple times (e.g. per section).' },
  { category: 'HTML', tag: 'semantic', item: 'nav', syntax: '<nav>...</nav>', description: 'Navigation links.', returnType: 'N/A', details: 'nav wraps major navigation (menus, TOC). Use for accessibility and semantics. Not every link group needs nav.' },
  { category: 'HTML', tag: 'semantic', item: 'main', syntax: '<main>...</main>', description: 'Main content of the page.', returnType: 'N/A', details: 'main should be one per page (or hidden duplicates). Skip nav/header/footer. Landmark for accessibility.' },
  { category: 'HTML', tag: 'semantic', item: 'section', syntax: '<section>...</section>', description: 'Thematic grouping of content.', returnType: 'N/A', details: 'section groups related content; usually has heading. Use when content has its own outline.' },
  { category: 'HTML', tag: 'semantic', item: 'article', syntax: '<article>...</article>', description: 'Self-contained content (e.g. post, card).', returnType: 'N/A', details: 'article is for content that could stand alone (blog post, comment). Can nest (e.g. comments in article).' },
  { category: 'HTML', tag: 'semantic', item: 'aside', syntax: '<aside>...</aside>', description: 'Tangentially related content (sidebar, pullquote).', returnType: 'N/A', details: 'aside is for content indirectly related to main (sidebars, ads, related links).' },

  { category: 'HTML', tag: 'head', item: 'title', syntax: '<title>Page Title</title>', description: 'Document title (tab, bookmarks).', returnType: 'N/A', details: 'title goes in head. Shown in browser tab and search results. One per page.' },
  { category: 'HTML', tag: 'head', item: 'meta charset', syntax: '<meta charset="UTF-8">', description: 'Character encoding for the document.', returnType: 'N/A', details: 'charset="UTF-8" in head for correct character display. Use early in head.' },
  { category: 'HTML', tag: 'head', item: 'meta viewport', syntax: '<meta name="viewport" content="width=device-width, initial-scale=1">', description: 'Mobile viewport scaling.', returnType: 'N/A', details: 'viewport controls width and zoom on mobile. width=device-width, initial-scale=1 is common for responsive.' },
  { category: 'HTML', tag: 'head', item: 'meta description', syntax: '<meta name="description" content="...">', description: 'Short description for search/social.', returnType: 'N/A', details: 'description often used in search results. Keep concise (e.g. 150–160 chars).' },
  { category: 'HTML', tag: 'head', item: 'link stylesheet', syntax: '<link rel="stylesheet" href="style.css">', description: 'Link external CSS.', returnType: 'N/A', details: 'rel="stylesheet", href = path to CSS. Placed in head. Multiple link tags for multiple stylesheets.' },
  { category: 'HTML', tag: 'head', item: 'script tag', syntax: '<script src="app.js"></script>  or  <script>code</script>', description: 'Load or inline JavaScript.', returnType: 'N/A', details: 'src = external script (blocking by default). No src = inline script. Use defer or async for non-blocking load.' },
  { category: 'HTML', tag: 'head', item: 'script defer and async', syntax: '<script src="app.js" defer></script>', description: 'When to run external script.', returnType: 'N/A', details: 'defer = run after HTML parsed, in order. async = run when loaded, order not guaranteed. Prefer defer for app scripts.' },

  { category: 'HTML', tag: 'text', item: 'p paragraph', syntax: '<p>...</p>', description: 'Paragraph of text.', returnType: 'N/A', details: 'p is block-level. One paragraph per p. Do not nest block elements inside p.' },
  { category: 'HTML', tag: 'text', item: 'h1 to h6 headings', syntax: '<h1>...</h1>  through  <h6>...</h6>', description: 'Heading levels; one h1 per page.', returnType: 'N/A', details: 'h1 = main title (one per page). h2–h6 = subheadings. Order matters for outline and accessibility.' },
  { category: 'HTML', tag: 'text', item: 'strong and em', syntax: '<strong>bold</strong>  <em>emphasis</em>', description: 'Strong importance and emphasis.', returnType: 'N/A', details: 'strong = strong importance (bold by default). em = emphasis (italic by default). Semantic; use over b/i when meaning matters.' },
  { category: 'HTML', tag: 'text', item: 'br line break', syntax: '<br>  or  <br />', description: 'Line break within text.', returnType: 'N/A', details: 'br is void (no content). Use sparingly; prefer block elements (p, div) or CSS for spacing.' },
  { category: 'HTML', tag: 'text', item: 'hr horizontal rule', syntax: '<hr>  or  <hr />', description: 'Thematic break (horizontal line).', returnType: 'N/A', details: 'hr is void. Semantic break between sections. Styled as line by default; override with CSS.' },

  { category: 'HTML', tag: 'details', item: 'details and summary', syntax: '<details><summary>Click to expand</summary>...</details>', description: 'Collapsible disclosure widget.', returnType: 'N/A', details: 'details wraps content; summary is the visible toggle. open attribute shows expanded by default. No JS required.' },
  { category: 'HTML', tag: 'details', item: 'summary element', syntax: '<summary>Title</summary>', description: 'Visible label for a details block.', returnType: 'N/A', details: 'First child of details. Click toggles open/closed. Only one summary per details.' },

  { category: 'HTML', tag: 'figure', item: 'figure element', syntax: '<figure>...</figure>', description: 'Self-contained content (image, diagram, code).', returnType: 'N/A', details: 'Groups content with optional figcaption. Use for images with captions, code blocks, diagrams.' },
  { category: 'HTML', tag: 'figure', item: 'figcaption', syntax: '<figure><img><figcaption>Caption</figcaption></figure>', description: 'Caption for a figure.', returnType: 'N/A', details: 'figcaption is first or last child of figure. Describes the figure content.' },

  { category: 'HTML', tag: 'quote', item: 'blockquote', syntax: '<blockquote cite="url">...</blockquote>', description: 'Block of quoted content.', returnType: 'N/A', details: 'blockquote = extended quote (block). cite attribute = source URL. Use p inside for paragraphs.' },
  { category: 'HTML', tag: 'quote', item: 'cite element', syntax: '<cite>Source</cite>', description: 'Citation or reference (e.g. title, author).', returnType: 'N/A', details: 'cite is inline. Use for book title, author, URL. Renders italic by default.' },
  { category: 'HTML', tag: 'quote', item: 'q inline quote', syntax: '<q>short quote</q>', description: 'Short inline quotation.', returnType: 'N/A', details: 'q = inline quote. Browser may add quotes. cite attribute for source URL.' },

  { category: 'HTML', tag: 'code', item: 'pre element', syntax: '<pre>...</pre>', description: 'Preformatted text (whitespace preserved).', returnType: 'N/A', details: 'pre keeps spaces and line breaks. Use for code, ASCII art. Monospace by default.' },
  { category: 'HTML', tag: 'code', item: 'code element', syntax: '<code>snippet</code>', description: 'Inline or block code snippet.', returnType: 'N/A', details: 'code = code (inline). Use <pre><code> for multi-line code. Monospace by default.' },
  { category: 'HTML', tag: 'code', item: 'pre and code together', syntax: '<pre><code>const x = 1;</code></pre>', description: 'Multi-line code block.', returnType: 'N/A', details: 'pre preserves formatting; code marks as code. Escape < and > as &lt; and &gt; inside.' },

  { category: 'HTML', tag: 'dl', item: 'dl definition list', syntax: '<dl><dt>Term</dt><dd>Definition</dd></dl>', description: 'Definition list (term + definition pairs).', returnType: 'N/A', details: 'dl = definition list. dt = term. dd = definition. Use for glossaries, key-value pairs.' },
  { category: 'HTML', tag: 'dl', item: 'dt and dd', syntax: '<dt>Term</dt><dd>Description</dd>', description: 'Term and definition in a dl.', returnType: 'N/A', details: 'dt = definition term. dd = definition description. Multiple dd per dt allowed.' },

  { category: 'HTML', tag: 'media', item: 'video element', syntax: '<video src="clip.mp4" controls></video>', description: 'Embed video.', returnType: 'N/A', details: 'src or source children. controls = play/pause. autoplay, muted, loop, poster (thumbnail). Optional <track> for captions.' },
  { category: 'HTML', tag: 'media', item: 'audio element', syntax: '<audio src="sound.mp3" controls></audio>', description: 'Embed audio.', returnType: 'N/A', details: 'src or source children. controls = play/pause. autoplay, muted, loop. No visual by default.' },
  { category: 'HTML', tag: 'media', item: 'source element', syntax: '<source src="file.mp4" type="video/mp4">', description: 'Alternative source for video/audio.', returnType: 'N/A', details: 'Use inside video or audio. src and type (MIME). Browser picks first supported. For multiple formats.' },

  { category: 'HTML', tag: 'other', item: 'address', syntax: '<address>Contact info</address>', description: 'Contact information for page or article.', returnType: 'N/A', details: 'address = contact (email, address, etc.). Usually in footer. Do not use for arbitrary addresses.' },
  { category: 'HTML', tag: 'other', item: 'mark', syntax: '<mark>highlighted</mark>', description: 'Highlighted or marked text.', returnType: 'N/A', details: 'mark = highlighted (e.g. search match). Yellow background by default. Semantic.' },
  { category: 'HTML', tag: 'other', item: 'abbr abbreviation', syntax: '<abbr title="World Wide Web">WWW</abbr>', description: 'Abbreviation or acronym; title for full form.', returnType: 'N/A', details: 'title attribute = full expansion. Dotted underline by default. Good for accessibility.' },
  { category: 'HTML', tag: 'other', item: 'progress', syntax: '<progress value="70" max="100">70%</progress>', description: 'Progress bar (task completion).', returnType: 'N/A', details: 'value = current; max = total. Omit value for indeterminate. Content is fallback.' },
  { category: 'HTML', tag: 'other', item: 'datalist', syntax: '<input list="suggestions"><datalist id="suggestions"><option value="A">', description: 'Suggestions for input (autocomplete).', returnType: 'N/A', details: 'datalist id linked by input list="id". option value = suggestion. User can type or pick.' },
  { category: 'HTML', tag: 'other', item: 'iframe', syntax: '<iframe src="url" title="Description"></iframe>', description: 'Embed another page (inline frame).', returnType: 'N/A', details: 'src = URL to embed. title required for accessibility. width, height, sandbox for security.' },

  // ========== Node.js & Modules ==========
  {
    category: 'Node.js & Modules',
    item: 'require()',
    syntax: 'const x = require("module");',
    description: 'CommonJS: load a module.',
    returnType: 'Exported value of the module',
    details: 'require() is synchronous. Returns whatever the module exports (module.exports).',
  },
  {
    category: 'Node.js & Modules',
    item: 'module.exports single',
    syntax: 'module.exports = xyz;',
    description: 'CommonJS: export a single value.',
    returnType: 'N/A',
    details: 'module.exports = xyz exports one value. Require returns that value directly.',
  },
  {
    category: 'Node.js & Modules',
    item: 'module.exports object',
    syntax: 'module.exports = { a, b };',
    description: 'CommonJS: export multiple named values.',
    returnType: 'N/A',
    details: 'module.exports = { a, b } exports an object. Require returns the object; use .a, .b to access.',
  },
  {
    category: 'Node.js & Modules',
    item: 'import (ES Modules)',
    syntax: 'import x from "module";  import { a, b } from "module";',
    description: 'ES Modules: load module or named exports.',
    returnType: 'N/A',
    details: 'import is static (top-level). import x for default export; import { a, b } for named exports.',
  },
  {
    category: 'Node.js & Modules',
    item: 'export (ES Modules)',
    syntax: 'export const a = 1;  export { a, b };',
    description: 'ES Modules: expose named values.',
    returnType: 'N/A',
    details: 'export exposes names. Can use export const/function or export { a, b } at end of file.',
  },
  {
    category: 'Node.js & Modules',
    item: 'export default',
    syntax: 'export default value;',
    description: 'ES Modules: single default export.',
    returnType: 'N/A',
    details: 'Only one default per module. Import with import name from "module" (any name).',
  },
  {
    category: 'Node.js & Modules',
    item: '__dirname',
    syntax: '__dirname',
    description: 'Absolute path of the directory containing the current file.',
    returnType: 'string (path)',
    details: '__dirname is available in CommonJS. In ES modules use import.meta.url and path/dirname to get directory. __dirname is not available in browser.',
  },
  {
    category: 'Node.js & Modules',
    item: '__filename',
    syntax: '__filename',
    description: 'Absolute path of the current file.',
    returnType: 'string (path)',
    details: '__filename is CommonJS only. In ESM use import.meta.url. Contains full path including filename.',
  },
  {
    category: 'Node.js & Modules',
    item: 'require vs import',
    syntax: 'require() at runtime  |  import at parse time',
    description: 'CommonJS is dynamic; ESM is static.',
    returnType: 'N/A',
    details: 'require() can be conditional or in middle of file. import is hoisted and must be at top; can use dynamic import() for async load.',
  },
  {
    category: 'Node.js & Modules',
    item: 'path module',
    syntax: 'const path = require("path");  path.join(a, b)  path.resolve(a)',
    description: 'Utilities for file paths.',
    returnType: 'string (path)',
    details: 'path.join() joins with OS separator. path.resolve() resolves to absolute. path.dirname(), path.basename(), path.extname().',
  },
  {
    category: 'Node.js & Modules',
    item: 'fs module',
    syntax: 'const fs = require("fs");  fs.readFileSync(path)  fs.writeFileSync(path, data)',
    description: 'Synchronous file read/write.',
    returnType: 'Buffer/string or void',
    details: 'fs.readFileSync returns file contents. fs.writeFileSync writes. Use async versions (fs.promises) in server to avoid blocking.',
  },

  // ========== Express.js ==========
  {
    category: 'Express.js',
    item: 'server.get',
    syntax: 'server.get(path, handler)',
    description: 'Register handler for GET requests to path.',
    returnType: 'N/A',
    details: 'server.get(path, handler) — handler runs when HTTP method is GET and path matches. Use for reading data.',
  },
  {
    category: 'Express.js',
    item: 'server.post',
    syntax: 'server.post(path, handler)',
    description: 'Register handler for POST requests to path.',
    returnType: 'N/A',
    details: 'server.post(path, handler) — handler runs when HTTP method is POST and path matches. Use for submitting data.',
  },
  {
    category: 'Express.js',
    item: 'server.put / patch / delete',
    syntax: 'server.put(path, handler)  server.delete(path, handler)',
    description: 'Register handlers for PUT, PATCH, DELETE.',
    returnType: 'N/A',
    details: 'Same pattern as get/post. put = full replace, patch = partial update, delete = remove resource.',
  },
  {
    category: 'Express.js',
    item: 'req.query',
    syntax: 'req.query',
    description: 'GET query parameters (from URL ?key=value).',
    returnType: 'Object',
    details: 'req.query holds parsed query string. For GET /search?q=hello, req.query.q is "hello". No middleware needed.',
  },
  {
    category: 'Express.js',
    item: 'req.body',
    syntax: 'req.body',
    description: 'POST (or PUT/PATCH) body data.',
    returnType: 'Object (when parsed)',
    details: 'req.body contains parsed body. Requires middleware: express.json() for JSON, express.urlencoded({ extended: true }) for form data.',
  },
  {
    category: 'Express.js',
    item: 'req.params',
    syntax: 'req.params',
    description: 'Route parameters from path (e.g. /user/:id).',
    returnType: 'Object',
    details: 'req.params has keys from route placeholders. For route /user/:id and URL /user/5, req.params.id is "5".',
  },
  {
    category: 'Express.js',
    item: 'req.headers',
    syntax: 'req.headers',
    description: 'Request headers (lowercased keys).',
    returnType: 'Object',
    details: 'req.headers["content-type"], req.get("Content-Type"). Read-only object of incoming headers.',
  },
  {
    category: 'Express.js',
    item: 'res.send()',
    syntax: 'res.send(body)',
    description: 'Send a response (text, HTML, Buffer, or object).',
    returnType: 'N/A',
    details: 'res.send() can send string (sets Content-Type text/html by default for strings), Buffer, or object (as JSON). Ends the response.',
  },
  {
    category: 'Express.js',
    item: 'res.json()',
    syntax: 'res.json(obj)',
    description: 'Send JSON response.',
    returnType: 'N/A',
    details: 'res.json() sets Content-Type to application/json and sends the object as JSON. Use for APIs.',
  },
  {
    category: 'Express.js',
    item: 'res.render()',
    syntax: 'res.render(view, locals)',
    description: 'Render a view template (e.g. EJS) and send HTML.',
    returnType: 'N/A',
    details: 'res.render() is for server-side templates (EJS, Pug). Pass view name and data object. Sends rendered HTML.',
  },
  {
    category: 'Express.js',
    item: 'res.redirect()',
    syntax: 'res.redirect(status, url)  or  res.redirect(url)',
    description: 'Redirect client to another URL.',
    returnType: 'N/A',
    details: 'res.redirect() sends 302 (or specified status) and Location header. Browser makes new request to that URL.',
  },
  {
    category: 'Express.js',
    item: 'res.status()',
    syntax: 'res.status(404).send("Not found")',
    description: 'Set HTTP status code.',
    returnType: 'Response (chainable)',
    details: 'res.status(code) sets status. Chain with send/json/render. 200 OK, 201 Created, 404 Not Found, 500 Server Error.',
  },
  {
    category: 'Express.js',
    item: 'express.static',
    syntax: 'express.static("public")',
    description: 'Serve static files from a directory.',
    returnType: 'Middleware function',
    details: 'express.static("public") serves files from the "public" folder. Request to /style.css maps to public/style.css. No URL path change unless mounted.',
  },
  {
    category: 'Express.js',
    item: 'express.urlencoded',
    syntax: 'express.urlencoded({ extended: true })',
    description: 'Parse URL-encoded form body (POST form data).',
    returnType: 'Middleware function',
    details: 'Middleware to parse application/x-www-form-urlencoded (form submit). extended: true uses qs library for nested objects. Populates req.body.',
  },
  {
    category: 'Express.js',
    item: 'express.json()',
    syntax: 'express.json()',
    description: 'Parse JSON request body.',
    returnType: 'Middleware function',
    details: 'Middleware to parse Content-Type: application/json. Populates req.body. Must use before routes that need req.body.',
  },
  {
    category: 'Express.js',
    item: 'app.use()',
    syntax: 'app.use(middleware)  or  app.use(path, middleware)',
    description: 'Mount middleware or router for all (or path-prefixed) requests.',
    returnType: 'N/A',
    details: 'app.use() runs for every request (or every request to path). Order matters; put body parsers and static before routes.',
  },

  {
    category: 'Express.js',
    item: 'Simple API setup (Express server)',
    syntax: 'const express = require("express");\nconst app = express();\nconst PORT = 3000;\napp.use(express.json());\napp.use(express.urlencoded({ extended: true }));\napp.listen(PORT, () => console.log(`Listening on ${PORT}`));',
    description: 'Minimal Express server with JSON and form body parsing.',
    returnType: 'N/A',
    details: 'Require express, create app, add express.json() and express.urlencoded for POST body. Call app.listen(PORT). Put middleware before routes.',
  },
  {
    category: 'Express.js',
    item: 'Simple GET API example',
    syntax: 'app.get("/api/items", (req, res) => {\n  const name = req.query.name;\n  res.json({ message: "Hello", name: name || "guest" });\n});',
    description: 'GET route that reads query params and returns JSON.',
    returnType: 'N/A',
    details: 'GET /api/items?name=Alice → req.query.name is "Alice". Use res.json(obj) to send JSON. No body parser needed for GET.',
  },
  {
    category: 'Express.js',
    item: 'Simple GET API with route param',
    syntax: 'app.get("/api/items/:id", (req, res) => {\n  const id = req.params.id;\n  res.json({ id });\n});',
    description: 'GET route with URL parameter (e.g. /api/items/5).',
    returnType: 'N/A',
    details: 'GET /api/items/5 → req.params.id is "5". Use :id in path; access via req.params.id. Params are always strings.',
  },
  {
    category: 'Express.js',
    item: 'Simple POST API example (JSON)',
    syntax: 'app.post("/api/items", (req, res) => {\n  const body = req.body;\n  res.status(201).json({ success: true, data: body });\n});',
    description: 'POST route that reads JSON body and returns 201 + JSON.',
    returnType: 'N/A',
    details: 'Requires app.use(express.json()) before routes. Client sends Content-Type: application/json. req.body is parsed object. Use res.status(201) for created.',
  },
  {
    category: 'Express.js',
    item: 'Simple POST API example (form)',
    syntax: 'app.post("/api/items", (req, res) => {\n  const name = req.body.name;\n  res.redirect("/");\n});',
    description: 'POST route that reads form data (e.g. from HTML form).',
    returnType: 'N/A',
    details: 'Requires app.use(express.urlencoded({ extended: true })). Form submit sends application/x-www-form-urlencoded. req.body has name=value pairs.',
  },
  {
    category: 'Express.js',
    item: 'Full simple API (GET + POST)',
    syntax: 'const express = require("express");\nconst app = express();\napp.use(express.json());\napp.get("/api/data", (req, res) => res.json({ items: [] }));\napp.post("/api/data", (req, res) => { res.status(201).json({ ok: true }); });\napp.listen(3000);',
    description: 'Complete minimal API: one GET and one POST route.',
    returnType: 'N/A',
    details: 'Order: require express, create app, use json middleware, define GET (read) and POST (create), then listen. GET uses req.query; POST uses req.body.',
  },
  {
    category: 'Express.js',
    item: '404 handler for API',
    syntax: 'app.use((req, res) => {\n  res.status(404).json({ error: "Not found" });\n});',
    description: 'Catch-all for unknown routes; return 404 JSON.',
    returnType: 'N/A',
    details: 'Put after all routes. No path = matches any request that did not match earlier routes. Use for APIs so client gets JSON, not HTML.',
  },

  // ========== EJS Templating ==========
  {
    category: 'EJS Templating',
    item: 'Escaped output',
    syntax: '<%= variable %>',
    description: 'Output variable value, HTML-escaped (safe from XSS).',
    returnType: 'N/A',
    details: '<%= variable %> escapes HTML so user input is safe. Use for any user-provided or untrusted data.',
  },
  {
    category: 'EJS Templating',
    item: 'Unescaped output',
    syntax: '<%- variable %>',
    description: 'Output raw HTML (no escaping).',
    returnType: 'N/A',
    details: '<%- variable %> does not escape — XSS risk. Only use when you trust the content (e.g. your own HTML).',
  },
  {
    category: 'EJS Templating',
    item: 'Logic tags',
    syntax: '<% logic %>',
    description: 'Execute JavaScript (loops, conditionals); no output.',
    returnType: 'N/A',
    details: '<% %> runs JS but does not output. Use for if, for, while, etc. Use <%= %> or <%- %> when you want to print.',
  },
  {
    category: 'EJS Templating',
    item: 'View engine setting',
    syntax: "server.set('view engine', 'ejs');",
    description: 'Set EJS as the default view engine.',
    returnType: 'N/A',
    details: "server.set('view engine', 'ejs') tells Express to use .ejs extension and EJS for res.render().",
  },
  {
    category: 'EJS Templating',
    item: 'Views directory',
    syntax: "server.set('views', path.join(__dirname, 'views'));",
    description: 'Directory where template files live.',
    returnType: 'N/A',
    details: "Default is ./views. res.render('page') looks for views/page.ejs when view engine is ejs.",
  },
  {
    category: 'EJS Templating',
    item: 'Include partials',
    syntax: '<%- include("header") %>',
    description: 'Include another EJS file (e.g. header, footer).',
    returnType: 'N/A',
    details: 'include() inserts the file. Use <%- include("partial") %> for raw output. Path relative to views folder.',
  },
  {
    category: 'EJS Templating',
    item: 'Pass locals to include',
    syntax: '<%- include("item", { item }) %>',
    description: 'Pass data into included template.',
    returnType: 'N/A',
    details: 'Second argument to include() is object of local variables. That partial can use item, etc.',
  },
  {
    category: 'EJS Templating',
    item: 'EJS for loop',
    syntax: '<% for (let i = 0; i < items.length; i++) { %>\n  <%= items[i] %>\n<% } %>',
    description: 'Loop over array by index in EJS.',
    returnType: 'N/A',
    details: 'Use <% %> for loop logic; use <%= %> inside to output each value. No output from <% %> tags.',
  },
  {
    category: 'EJS Templating',
    item: 'EJS for...of loop',
    syntax: '<% for (const item of items) { %>\n  <%= item %>\n<% } %>',
    description: 'Loop over array values in EJS.',
    returnType: 'N/A',
    details: 'for...of gives each element. Use <%= item %> to render. Same pattern: logic in <% %>, output in <%= %>.',
  },
  {
    category: 'EJS Templating',
    item: 'EJS forEach loop',
    syntax: '<% items.forEach(function(item) { %>\n  <%= item %>\n<% }); %>',
    description: 'Loop using forEach in EJS.',
    returnType: 'N/A',
    details: 'items.forEach(cb) — inside <% %>. Use <%= item %> in body to output. Arrow: items.forEach(item => { %> ... <% });',
  },
  {
    category: 'EJS Templating',
    item: 'EJS if / else',
    syntax: '<% if (user) { %><%= user.name %><% } else { %>Guest<% } %>',
    description: 'Conditional output in EJS.',
    returnType: 'N/A',
    details: 'if/else in <% %>; output with <%= %> in each branch. No output from the if/else tags themselves.',
  },
  {
    category: 'EJS Templating',
    item: 'EJS if / else if / else',
    syntax: '<% if (x > 0) { %>positive<% } else if (x < 0) { %>negative<% } else { %>zero<% } %>',
    description: 'Multiple conditions in EJS.',
    returnType: 'N/A',
    details: 'Same as if/else; add else if (condition) { } blocks. Use <%= variable %> when you need to output a value.',
  },
];
